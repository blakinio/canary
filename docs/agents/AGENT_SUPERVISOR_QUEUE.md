# Agent Supervisor Queue

ACO-004 defines a deterministic, bounded queue contract for external or higher-license multi-agent execution.

The queue is a planning and handoff layer only. Ordinary Chat does not spawn workers, and this repository tooling does not create unrestricted execution processes.
