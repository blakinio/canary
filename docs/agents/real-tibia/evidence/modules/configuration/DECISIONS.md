# Configuration evidence decisions

## RTEC-005 wave 3 candidate

- Use only the preflight-pinned current-Canary configuration paths.
- Classify the candidate as `PROVEN` at `runtime-path-proven`, not behavior/gameplay/parity proven.
- Keep the module index and version history empty while the record is `review-needed`.
- Do not open an owner request: missing proof concerns broader deployment/runtime behavior rather than a narrow owner-controlled artifact required by this candidate.
- Preserve explicit nonclaims for production values, secrets, controlled feature correctness, protocol/client behavior and parity.

## Publication rule

Only the wave 3 coordinator may accept or narrow the record, populate module/global indexes and update the programme handoff.
