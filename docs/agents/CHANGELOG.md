# Agent-Facing Change Log

Curated behavior/architecture log for discovery; not a replacement for Git history or release notes.

## Unreleased

- Adds `canary-otbm-critical-access-targets-v1` and `canary-otbm-critical-access-integrity-v1` as a fail-closed current-state integrity composer for explicitly reviewed critical landmarks, houses and spawn/NPC/boss access contexts. The tool reuses the exact Semantic Landmark resolver, canonical World Index house-door evidence, existing QA-011 Connectivity Resilience routes, Geometry Audit house findings and Spawn/NPC validation placement IDs without reparsing OTBM, pathfinding, recomputing geometry or rescanning datapacks; criticality and public-access intent remain reviewed metadata, truncated/missing/stale evidence stays unresolved or conflicting, and static results never prove runtime/public accessibility or infer before/after entrance bypass/sever changes without separately compatible Semantic Diff evidence.
- Adds `canary-otbm-connectivity-resilience-manifest-v1` and `canary-otbm-connectivity-resilience-v1` as a bounded reviewed-route analysis layer over the existing canonical OTBM Reachability graph/BFS and reviewed transition model. The analysis keeps strict, optimistic and optional interaction-aware executable evidence separate; proves single-edge route fragility by rerunning the same canonical `_bfs()` with exactly one predecessor-route edge removed; reports one proven edge-disjoint alternative only when the canonical BFS still reaches the goal after excluding the first path; identifies selected entry points with no proven selected exit as static entrapment review candidates; and summarizes one-way, dead-end and cycle topology without creating a second pathfinder, inferring transition destinations, executing Lua/runtime state or claiming global connectivity/runtime entrapment.
- Adds `canary-otbm-quest-state-reachability-manifest-v1` and `canary-otbm-quest-state-reachability-v1` as a conservative selected-scope composition over exact existing Storage Dependency Graph transition IDs, reviewed initial/goal states, exact map-context evidence and optional reuse of the existing Route Interaction resolver. Proven predecessor chains distinguish `reachable`, `blocked-by-evidence`, `unreachable-in-selected-scope` and `external-or-unproven` without reconstructing storage parsing, inferring Lua/callback order, expanding inequalities or `else`, running pathfinding, claiming global impossibility or treating static evidence as runtime quest completion.
- Binds the existing `canary-otbm-semantic-landmarks-v1` registry to evidence-reviewed `thais.temple` and `thais.depot` anchors for the exact pinned baseline map: source-map and canonical World Index SHA-256 provenance are fixed, Thais town metadata and exact `depotId=8` locker placements are preserved, and the depot route destination is selected deterministically from strict-walkable Reachability evidence rather than guessed. This remains static evidence only; the downstream OTBM-E2E-005 controlled physical OTClient route is still required for gameplay proof.
- Adds reusable Universal Physical E2E `follow_route` execution over canonical runner-owned `route-<logical-id>.json` `canary-otbm-e2e-route-plan-v1` artifacts: fail-closed hash, provenance, executability, path-continuity and interaction validation occurs before controlled-client execution; ordinary movement reuses the single exact `walk_edge` executor with exact source, one derived movement request and exact destination synchronization; reviewed `use-map-item`, verified `use-inventory-on-map`, and transition activations use exact targets/source/destination, bounded timeouts, deterministic first-failure diagnostics and route-step evidence. No arbitrary route path, second runner/workflow/parser/World Index/pathfinder, map/client asset or reference Thais route is added; exact-map static preflight remains OTBM-E2E-004 and the reference physical route remains OTBM-E2E-005.
- Adds `canary-otbm-route-interactions-v1` as the reviewed fail-closed bridge from exact OTBM transition/mechanic evidence to physical activation semantics: stable transition IDs or exact positions plus item/AID/UID/house-door identifiers resolve deterministically to `step-on`, `walk-direction`, `use-map-item` or `use-inventory-on-map`; source-map/World Index and optional transition-manifest/Script Resolution provenance is SHA-256-pinned; unknown or ambiguous mechanics remain blocked; and handler-gated interactions cannot whitelist `partially-resolved`, `referenced-only`, `unresolved` or `conflicting` evidence. The registry does not modify Reachability, add another pathfinder, execute the physical client or treat static resolution as gameplay proof.
- Extends `canary-otbm-e2e-route-plan-v1` with optional reviewed interaction-aware executable routing while preserving the existing OTBM Reachability BFS and predecessor graph as the sole pathfinder: geometry `routeStatus`, `strictDistance` and `optimisticDistance` remain authoritative; supplying an exact SHA-pinned `canary-otbm-route-interactions-v1` registry builds a narrower fail-closed executable edge set and reruns the same `_bfs()`, emitting `executableDistance`, complete ordered movement/transition edges and exact edge-level interaction resolutions. Plain optimistic walkability is never physical executability; unknown appearances, missing or unresolved dynamic/script evidence, ambiguous or unsupported interactions and unreviewed transitions remain blocked; removing required interaction evidence removes the executable route; and routes exceeding the supported full-path bound still fail closed with empty executable path/edges rather than publishing a head/tail sample. Static executable resolution does not prove runtime gameplay state and adds no E2E runner or second pathfinder.
- Adds the versioned `canary-otbm-semantic-landmarks-v1` registry and standard-library validator/resolver for evidence-reviewed semantic route anchors: reviewed registries pin exact source-map and World Index SHA-256 values, declare inclusive 3D routing regions and role-specific exact anchors, reject duplicate, unknown, out-of-region, ambiguous and stale data fail-closed, and never infer coordinates from names, sprites or labels. The committed registry intentionally starts `unbound` with no landmarks until real-map evidence is reviewed.
- Adds exact-position-synchronized Universal Physical E2E `walk_edge` execution for ordinary same-floor route movement: scenarios declare one adjacent evidence-backed `P0 -> P1` edge in OTBM coordinate range, validation derives direction from the coordinate delta rather than accepting caller direction, the controlled real OTClient must be at the exact source before sending exactly one walk request, and execution waits for the exact destination while failing closed on rejected movement, timeout or third-position drift. The existing bounded `walk`, persistence assertions and two-session safe-logout/relog lifecycle remain backward compatible; the physical movement fixture reuses coordinates already proven by real-client evidence in merged PR #481. This is a movement primitive only and does not implement full `follow_route`, semantic landmarks, transition interactions, static route preflight or pathfinding.
- Defines the next-phase Universal Physical E2E gameplay-validation architecture and ordered roadmap: one canonical disposable Canary/MariaDB/controlled-OTClient platform remains the runtime authority; static OTBM evidence and the separate OTBM-to-E2E routing programme own map truth and route planning; feature programmes own deterministic scenarios and expected values; and evidence maturity is separated into static, physical-action, feature-outcome, persistence, cross-actor/system and recovery levels. The queue covers OTBM-aware route consumption, quest/NPC and combat vertical slices, reusable persistence assertions, justified multi-client orchestration, controlled runtime fault/recovery validation and cross-system gameplay journeys without creating a second E2E orchestrator, workflow, parser or pathfinder.
- Adds a bounded raw OTBM tile type conversion materializer for separately approved `OTBM_TILE ↔ OTBM_HOUSETILE` conversion at exact same absolute positions and canonical parent `TILE_AREA` keys. Conversion copies complete reviewed donor raw subtrees rather than patching node-type bytes or synthesizing house metadata; approval pins both maps, World Indexes/manifests and exact current/donor raw plus canonical state; and the result proves exact non-selected current-byte retention, donor raw equality, native reparse, rebuilt selected canonical World Index equality and bounded Semantic Diff before create-new publication. v1 does not translate coordinates, create/delete TILE_AREA parents, insert/delete tiles, edit item stacks, serialize arbitrary nodes, write sources in place, invent house IDs, or claim gameplay/physical-client correctness.
- Adds a bounded raw OTBM tile deletion materializer for separately approved deletion of complete existing `OTBM_TILE` or `OTBM_HOUSETILE` subtrees at exact reviewed absolute positions. Approval pins the current map, World Index/manifest and each selected raw plus canonical tile state; deletion removes only complete scanner-proven spans while preserving the parent `TILE_AREA` even when empty; and the result proves the output is byte-for-byte the current map with exactly selected spans omitted, then requires native reparse, selected raw/World Index absence and bounded Semantic Diff before create-new publication. v1 does not delete parent TILE_AREA nodes, insert/replace tiles, translate coordinates, convert tile/house types, edit item stacks, serialize arbitrary nodes, write the source in place, or claim gameplay/physical-client correctness.
- Adds a bounded raw OTBM tile insertion materializer for separately approved insertion of complete donor `OTBM_TILE` or `OTBM_HOUSETILE` subtrees at exact same absolute positions that are absent from current but whose canonical parent `TILE_AREA` already exists exactly once. Approval pins both maps, World Indexes/manifests, each reviewed current parent area raw hash/length and donor raw plus canonical tile state; insertion copies donor raw bytes immediately before the existing parent node end in deterministic position order; and the result proves the complete current byte sequence is preserved after excluding only inserted spans, donor raw equality, native reparse, rebuilt inserted-tile World Index equality and bounded Semantic Diff before create-new publication. v1 does not create TILE_AREA parents, replace/delete tiles, translate coordinates, edit item stacks, serialize arbitrary nodes, write sources in place, or claim gameplay/physical-client correctness.
- Adds a bounded raw OTBM tile materializer for separately approved replacement of already-existing complete `OTBM_TILE` or `OTBM_HOUSETILE` subtrees at exact same absolute positions, canonical parent `TILE_AREA` keys and node types. The existing native scanner wrapper gains physical tile-span evidence only after the canonical scanner accepts the full map; approval pins both maps, World Indexes/manifests and exact current/donor raw plus canonical state for every selection; and the result proves donor raw-subtree equality, exact non-selected current-byte retention, native reparse, rebuilt selected-tile World Index equality and bounded Semantic Diff before create-new publication. v1 does not insert/delete tiles, translate coordinates, convert tile/house types, edit item stacks, serialize arbitrary nodes, write sources in place, or claim gameplay/physical-client correctness.
- Adds a bounded OTBM tile-area materializer for explicitly approved, same-coordinate complete `OTBM_TILE_AREA` structural updates. A same-translation-unit native scanner wrapper reuses the existing scanner implementation to expose physical area spans; `canary-otbm-area-materialization-approval-v1` separately pins the read-only region-plan SHA, every aligned area key and every non-blocking conflict; and `canary-otbm-area-materialization-result-v1` proves donor-subtree byte equality, exact retention of non-selected current bytes, native reparse, rebuilt World Index selected-area equality, bounded Semantic Diff, and unchanged current/donor/scanner pins before publishing a create-new copy. v1 requires `replace-region`, zero translation and complete 256x256 tile areas; it does not expand Phase 8, serialize a full map, perform tile-level overlay or translated fragment import, modify source maps in place, or claim gameplay correctness.
- Adds the OTS Security Validation Platform foundation: a strict product-neutral source-regression registry and Python standard-library source-regex executor with explicit repository authorization, path/symlink/size confinement, deterministic SHA-256 evidence reports, and permanent regressions for the merged `FS.mkdir` shell-execution and `table.unserialize` arbitrary-evaluation fixes; future runtime offensive adapters must reuse Universal OTS E2E and authorized isolated targets rather than adding arbitrary manifest commands or a second runtime stack.
- Adds the first code-owned OTS Security Validation runtime delegation adapter: strict adapter manifests resolve an existing Universal OTS E2E scenario through exact `blakinio/canary` authorization, literal-loopback-only target confinement and an approved `blakinio/otclient` control-plane contract, while deterministic `ots-security-runtime-delegation-v1` evidence SHA-256-pins the Universal E2E workflow, resolver, physical runner and selected scenario. The adapter delegates rather than duplicating lifecycle and does not yet execute malformed packets, fuzzing or database-abuse probes.
- Adds OTS-SEC-003 bounded exact-head runtime resilience checks for common TCP framing and unauthenticated `ProtocolStatus`: eight fixed code-owned cases execute against disposable MySQL + Canary through the reused Universal Agent Load runtime callback, distinct deterministic loopback sources isolate malformed and control probes while existing admission/status-query throttles remain enabled, and deterministic SHA-256 evidence plus process/fatal diagnostics fail closed. The pack does not claim authenticated login/game, XTEA, checksum/sequence, hostile-server client parsing or sustained-DoS coverage.
- Adds OTS-SEC-004 bounded exact-head login protocol boundary validation: six fixed code-owned cases cover unsupported-version, incomplete first-message and bounded login-handshake rejection/negative-authentication paths; a protocol-aware control proves the login service remains responsive after every case; deterministic isolated loopback sources preserve normal admission throttles; and machine-readable evidence pins the exact Canary binary, driver, runtime provider and bounded responses. The pack does not claim successful account authentication, character-list correctness, game-session establishment, post-login transport coverage, hostile-server client parsing or sustained-DoS capacity.
- Adds bounded declarative physical-gameplay action plans to the existing Universal OTS E2E platform: strict optional `scenario.steps` validation and deterministic `scenario-plan.lua` generation feed a controlled real-OTClient driver with bounded movement, talk/spell text, visible-target attack, inventory-item use, quest/channel requests and state observations while preserving the canonical two-session login/safe-logout/persistence/relog sentinel. The platform records deterministic step markers, rejects unknown/unbounded/unsafe actions fail-closed, keeps concrete coordinates/item IDs/NPCs/monsters in feature-owned deterministic fixtures, and does not create a second E2E orchestrator or weaken physical-client evidence.
- Adds feature-neutral typed Universal Physical E2E persistence assertions on the existing two-session orchestrator: feature-owned `scenario.assertions.persistence` `player_field` checks for directly comparable `level` and `experience` values are validated once, rendered into phase-two controlled-OTClient re-verification after relog, and compiled into the existing scalar SQL path after the second safe logout. Raw server vocation IDs are intentionally deferred because the controlled client exposes a different vocation representation; supporting vocation requires an explicit normalization contract rather than numeric-identity assumptions. The platform keeps raw `assertions.sql` backward compatible, exposes no arbitrary SQL through the typed contract, preserves feature-owned expected values, and adds no runner, workflow or OTBM routing logic.
- Adds fail-closed incremental PR validation for the main CI, Universal Agent Load and Universal Agent E2E workflows: a proven non-impacting single-commit delta may reuse only the immediate parent's latest successful same-workflow pull-request evidence, while missing/non-success evidence, impacting paths and workflow/helper changes force full validation. Current-head focused checks and stable Required aggregators remain active, and the `ci:final-gate` label forces the full applicable validation set on the exact final PR head before merge without weakening tests, branch protection, runtime sentinels or safety checks.
- Makes Global datapack Gameplay Analytics library bootstrap deterministic and filesystem-order-independent: any public entrypoint can initialize the existing core/context/schema/batching/reliability/correctness stack in its established order, repeated public loads are idempotent, and explicit decoration of a pre-existing analytics table retains the prior isolated-extension behavior used by focused tests. Original implementation bodies remain byte-for-byte preserved behind auto-skipped `#` paths, existing validators inspect those preserved implementations, and no schema, persistence, retry, batching, correctness or fail-closed runtime-smoke assertion is weakened.
- Adds a read-only OTBM donor/region merge planner over canonical current/donor World Index inputs with an explicit donor region and declared translation, deterministic `overlay`/`replace-region` review actions, exact current/donor/proposed tile snapshots, global UID/AID/house-door/teleport conflict evidence, optional script-resolution compatibility and bounded samples with exact totals. The `canary-otbm-region-merge-plan-v1` contract keeps `writerReady` false, fails closed on incompatible/incomplete index evidence or coordinate overflow, never heuristically aligns regions, never writes OTBM or expands Phase 8, and never promotes unresolved runtime evidence to handled or structural review evidence to gameplay correctness.
- Adds an OTBM repair sandbox verifier that reuses the existing Phase 8 bounded patcher on a distinct artifact copy, re-verifies its evidence manifest, runs real before/after item audits and script resolution, proves exact expected-to-replacement placement identity and untargeted mechanic preservation, and emits structural runtime-resolution diffs without claiming reachability, gameplay correctness, player intent or physical-client E2E.
- Adds a read-only OTBM static map quality gate that combines compatible `canary-otbm-geometry-audit-v1`, `canary-otbm-reachability-v1` and `canary-otbm-script-resolution-v1` reports only when all three prove the same source-map SHA-256. The new `canary-otbm-map-quality-v1` contract keeps `error`, `warning`, `unresolved` and `info` separate, preserves runtime unresolved/referenced-only/partially-resolved evidence, uses exact component summary counts with deterministic bounded samples, pins every input report, exposes independent severity/unresolved fail policy, and uses exclusive create-new publication unless overwrite is explicit. It never parses or writes OTBM, deduplicates component evidence into inferred defects, or claims gameplay correctness, player intent or global map coverage.
- Adds and hardens a read-only OTBM real-map repair preflight that reuses the existing item/mechanic audit, native Phase 8 patch anchors and active Lua/XML script-resolution audit to locate exact mechanic candidates, preserve unresolved/conflicting evidence, reject ambiguous placements, and optionally emit a review-only existing-attribute `canary-otbm-bounded-patch-plan-v1` draft. The `canary-otbm-repair-preflight-v1` report now separates selector matching from exact correlation/runtime/patch/review readiness, pins appearances/items/rules/script-corpus/Git evidence deterministically, and compares hypothetical resolver evidence structurally so same-status handler/source/registration changes are visible. The preflight never parses or writes OTBM independently, never executes the patcher, protects the source map from report-path collisions, and does not claim gameplay correctness or player intent.
- Adds an OTBM repair/materialization finalization pipeline that composes the existing repair sandbox or complete same-coordinate TILEAREA materializer with the existing Map Quality Gate. It SHA-256-pins explicit direct file inputs, replays exactly one reviewed/approved mutation into an internal candidate, requires explicit compatible Geometry/Reachability/Script Resolution evidence to resolve to that exact candidate SHA-256, preserves unresolved findings, proves source immutability, and publishes only a byte-identical create-new final map plus deterministic `canary-otbm-repair-materialization-pipeline-v1` evidence after a green fail-closed quality gate. It adds no parser, World Index, Semantic Diff, script resolver, renderer, structural writer, approval format, E2E lifecycle or deployment path and does not claim gameplay correctness or physical-client behavior.
- Repairs the context-handoff pipeline so the standard task template includes the authoritative `## Context checkpoint`, repository-relative task/config paths resolve from the repository root, and checkpoint-less legacy tasks produce an explicit recovery warning with frontmatter-derived head/branch/normalized PR/status plus a safe checkpoint-reconstruction next action. Strict checkpoint validation remains fail-closed, evidence lists are never inferred from legacy prose, and REQUIRED_READS/EVIDENCE_BUNDLE now share one canonical PR identity.
- PR #393 adds a reusable literal-loopback-only Canary status-protocol load/stress layer beside the existing physical real-OTClient E2E sentinel: bounded JSON profiles, a standard-library load runner, P50/P95/P99/max latency and throughput/error evidence, exact-head Canary runtime reuse, a dedicated workflow, and narrow synchronization around the existing process-wide `ProtocolStatus::ipConnectMap` throttle check/update. The layer preserves normal per-IP throttles, never targets production or third-party hosts, and does not claim authenticated gameplay-player capacity.
- Adds ACO-002 changed-task and lifecycle automation: active task records changed by a PR are checkpoint-validated and bound to that PR without forcing an immediate migration of untouched legacy records; deterministic exact-`related_pr` archival moves merged tasks out of `tasks/active`; and a trusted `pull_request_target` workflow checks out only the default branch, opens a normal cleanup PR, and enables auto-merge under existing CI and branch protection. Generic automation does not execute contributor heads, push directly to protected `main`, or infer edits to free-form program records.
- Adds budget-aware agent context orchestration: machine-readable context routes, deterministic checkpoint validation, compact resume/evidence bundles, and CHAT/CODEX/WORK execution-mode advice with `minimize_agentic_usage` as the default. CHAT remains preferred for analysis and connector-based GitHub/PR/CI work; CODEX and WORK receive bounded context only when local execution or broad research/large deliverables materially justify scarce agentic usage, then return coordination to CHAT. The tooling uses Python 3.12 standard library only and does not assume ordinary Chat can spawn external agents.
- Defines the concrete first-version Oteryn server architecture as a single-world, single-channel modular monolith with explicit engine, platform/persistence, protocol, scripting and game-domain ownership; a convergence target for repository/file layout; controlled startup/shutdown and authoritative mutation boundaries; module contract requirements; and semantic three-way evaluation of pinned upstream Canary, legacy `blakinio/canary` and CrystalServer evidence. `instances` and fork-specific `multichannel` are explicitly deferred from initial core and first migration waves. The 62-module registry remains the sole module inventory, all migration dispositions remain `REVALIDATE`, OAM-002 remains blocked, and no runtime, gameplay, database, protocol implementation, client, Lua/datapack, map, OTBM or asset behavior changes.
- Adds `CAN-PROGRAM-OTERYN-ARCHITECTURE-AND-MIGRATION` and a documentation-only Oteryn target architecture and migration contract. The 62-module Real Tibia registry remains the sole migration decision inventory, all modules remain `REVALIDATE` until target-side evidence supports a stronger disposition, and the contract defines `REUSE`/`ADAPT`/`REWRITE`/`DO_NOT_MIGRATE`/`EXPERIMENTAL_ONLY` gates, exact baseline/provenance rules, dependency-aware sequencing, and mandatory reuse of Upstream Intelligence, Universal Physical-Client E2E and existing OTBM tooling. No Oteryn repository, default branch or target SHA is invented; target implementation remains blocked until an authorized target repository and exact baseline exist. No runtime, gameplay, database, protocol implementation, client, map, OTBM or asset behavior changes.
- Adds bounded TSD-005 discovery records for `combat-conditions` and `weapons`, increasing the Real Tibia registry from 39 to 41 modules while preserving `combat`, `spells`, `vocations`, `weapon-proficiency`, character progression, player persistence and protocol. Target selection, permissions, formulas, damage/healing, mitigation, ordering, areas, chains and critical/leech stay inside the existing combat umbrella; condition subtypes stay inside one condition lifecycle; melee, distance and wand implementations stay inside one weapon lifecycle. No targeting, formula, condition, weapon, runtime, protocol, client or E2E correctness claim is made.
- Adds bounded TSD-004 discovery records for `bestiary`, `bosstiary`, `cyclopedia-character` and `titles`, increasing the Real Tibia registry from 35 to 39 modules while preserving `cyclopedia` as the broad umbrella and keeping `charms`, `houses`, `achievements`, `protocol`, character lifecycle/progression and player persistence unchanged. Cyclopedia Items and Map remain umbrella/protocol/client surfaces, Cyclopedia Houses stays covered by `houses`, and appearance unlocks remain deferred. No Bestiary/Bosstiary formula, persistence, title-unlock, protocol, maintained-client, runtime or E2E correctness claim is made.
- Adds bounded TSD-003 discovery records for `account-lifecycle`, `account-authentication`, `character-lifecycle`, `character-progression`, `vocations` and `weapon-proficiency`, increasing the Real Tibia registry from 29 to 35 modules while preserving `player-persistence`, `protocol`, `achievements` and `wheel-of-destiny`. Account entitlements stay an account-lifecycle capability, sanctions are deferred to TSD-009, progression fields remain one compatibility umbrella, individual vocation entries remain one registry lifecycle, and titles/appearance unlocks are deferred. No authentication, persistence, formula, runtime, protocol, client or E2E correctness claim is made.
- Adds bounded TSD-002B discovery records for `database-connection`, `database-migrations` and `world-persistence`, increasing the Real Tibia registry from 26 to 29 modules while preserving `player-persistence` as the unchanged umbrella. `transaction-boundaries` remains a DB-core/call-site capability, `database-reconciliation` is deferred, and `save-restart-reload` remains a cross-module validation package. No schema, migration, runtime, serializer, workflow or database implementation changes are included, and no atomicity, rollback, idempotency, crash-safety or restart-safety claim is made.
- Adds bounded TSD-002A engine-foundation discovery records for `build-system`, `engine-scheduler`, `engine-service-container` and `lua-bindings`, increasing the Real Tibia registry from 22 to 26 modules; `data-registries` remains distributed across functional modules and startup lifecycle, while `platform-compatibility` remains a build-system capability. The package changes no existing module record, persistence boundary, schema, generator, mapper, workflow behavior, gameplay or runtime code.
- Makes Upstream Intelligence module mapping source-role aware through explicit `module_mapping.path_buckets` policies in the existing source registry: server/donor sources cannot consume client buckets, the upstream client cannot consume server buckets, RME and Client Editor have separate editor policies, and missing/unsupported source context remains explicitly unmapped; mapping stays deterministic and discovery-only and does not change triage, reviewed decisions, module records, `protocol.yaml`, runtime or external repository state.
- Adds the bounded `CAN-PROGRAM-TIBIA-SYSTEM-DECOMPOSITION` bootstrap over the existing Real Tibia registry: umbrella/child conventions without a schema fork, classification of all requested candidates, three conservative engine-foundation records (`engine-runtime-lifecycle`, `configuration`, `lua-runtime`), deterministic multi-module discovery tests, an Oteryn classification workflow and an exact TSD-002 handoff; no gameplay/runtime, protocol, database, client, map, datapack or asset behavior changes.
- Adds read-only Upstream Intelligence and Drift Tracking for OpenTibiaBR Canary/OTClient, CrystalServer, Remere's Map Editor and Client Editor: exact observed heads, bounded rolling-window commits/PRs/issues/releases, conservative module mapping, revision-pinned decisions, target-HEAD-only local evidence, escaped Markdown reports, immutable artifacts, one stable local report issue, daily/weekly schedules and no automatic import or external write path.
- Adds the Real Tibia module registry-as-code foundation: one conflict-resistant YAML/JSON record per module, formal category/source/baseline/module schemas, multidimensional maturity and freshness, deterministic dependency/path/source/generated indexes, path and affected-module lookup, focused validation tests, reusable parity templates, an ADR-backed generated-file policy and dedicated CI. The initial 18-module inventory is discovery metadata only and does not claim gameplay parity.
- PR #326 removes command-shell execution from `FS.mkdir` and `FS.mkdir_p`: compatibility wrappers now call native `FileSystem.createDirectory`/`createDirectories` bindings backed by `std::filesystem` and `std::error_code`; standalone Lua and real-binding C++ regressions cover existing paths, failures and literal shell metacharacters without marker creation.
- PR #322 adds Phase 7 bounded read-only OTBM geometry and consistency analysis over canonical World Index and appearances evidence: exact missing-floor, duplicate-ground, component, house/PZ and low-confidence invisible-blocker candidates; reviewed manifest-backed wall/border adjacency; deterministic findings and factual render requests through the existing renderer; no OTBM parsing/writing, map modification, AI imagery or gameplay repair.
- PR #319 fixes bounded Semantic OTBM Diff across multiple 256x256 World Index areas by heap-merging area-local iterators in strict `(z,y,x)` order; the fail-closed non-increasing-position check remains active and a two-area synthetic OTBM regression covers the real Targuna-audit failure.
- Adds repository-wide Real Tibia parity governance: a mandatory evidence and delivery playbook, a global module-program registry, a durable Wheel of Destiny queue, exact source/proof-level requirements, bounded finding/task lifecycle, CI evidence and durable handoff for every module compared with Real Tibia or donor implementations, and archival of the historical broad Wheel #230 task as superseded rather than treating its title as proof of full 15.25 parity.
- PR #311 adds Phase 6 read-only Semantic OTBM Diff over two compatible canonical World Index inputs: exact tile, stack, AID/UID/house-door/teleport and Phase 3 walkability changes; deterministic full or bounded comparison with exact counts and stable IDs; conservative optional Phase 2–5/script correlation; atomic/path-confined output; and factual before/after/context render manifests through the existing renderer without reparsing or modifying OTBM, committing private artifacts or using AI-generated map imagery.
- PR #304 makes the Instanced Test Arena a real gameplay scenario: `InstanceArenaService::enterArena()` spawns and registers one real monster (Cave Rat) in the reserved region, rolling the whole arena back on any spawn/register failure, and a cleanup callback empties the instance's creature registry before `close()` so the existing "no registered creatures at close" invariant holds. `Game::getInstanceCreatureBinder()` wires the engine's one production summon-creation call site (`Monster::onThinkDefense()`) to the already-tested instance-aware `Creature::setMaster(master, binder, reload)` overload, so summons made by an instance-owned monster inherit its owner; normal-world summons are unaffected.
- PR #295 adds the admin-gated `/instancearena create|leave|close` talkaction and matching `Game.createInstanceArena`/`leaveInstanceArena`/`closeInstanceArena` Lua bindings on top of `InstanceArenaService`, plus its per-player enter/leave/close session API keyed by stable player id.
- PR #289 adds `InstanceArenaService`, the Instanced Test Arena program's real consumer of the merged `InstanceManager` foundation: two fixed, evidence-backed map regions, and create/activate/close arena lifecycle owned by `Game::getInstanceArenaService()`.
- PR #287 fixes a stale claim in `docs/architecture/instance-manager.md` (the Game-ownership decision already has an owner) and adds `docs/architecture/instanced-test-arena.md`, the OTBM-evidence-backed region plan for the Instanced Test Arena program.
- PR #299 adds Phase 5 read-only storage dependency analysis on top of the Quest Map Validator: SHA-256-verified selected files are inventoried across player/account/global storage, KV namespaces and narrow literal SQL evidence; exact same-key equality branches can produce literal/delete/delta transitions; dynamic expressions remain unresolved; source proximity never implies execution order; reports are bounded, atomic and optionally correlate Phase 2–4 evidence without changing maps, datapacks, gameplay or persistence.
- PR #297 normalizes zero-level `ConditionLight` state at construction/start and deserialization boundaries, preventing integer division by zero while preserving valid light levels and fade intervals.

- PR #286 adds Phase 4 read-only spawn, boss and NPC validation: explicit active companion XML and definition evidence, current loader coordinate/radius/interval semantics, separate reward-boss and Bosstiary spawn-boss classification, conservative literal dynamic-creation inventory, exact static/dynamic overlap findings, and bounded World Index plus Phase 3 geometry correlation without executing Lua or modifying map/gameplay data. Its first active global datapack scan observed 52,903 groups, 84,294 placements, 2,688 definitions and 461 dynamic creation calls; duplicate active Harlow definitions and the resulting ambiguous placement remain review evidence rather than automatic repair.
- PR #274 adds the read-only Phase 3 OTBM teleport and reachability validator: bounded strict/optimistic walkability from World Index plus real appearance flags, automatic indexed teleport checks, reviewed non-teleport floor-transition manifests, no-corner-cut pathfinding, route/mechanic reachability, one-way/dead-end/cycle findings, optional script-resolution evidence, atomic reports and a local toolkit workflow without committing maps, indexes or client assets.
- Added bounded Forge server-authority policies and regressions in PR #250 so crafted normal Fusion, Convergence Fusion and Convergence Transfer requests are rejected before item/resource/history mutation; full Forge parity remains tracked by `CAN-PROGRAM-EQUIPMENT-UPGRADE-PARITY`.
- PR #251 aligns the Imbuement registry with the selected current live Tibia mechanics: fixed application fees of 7,500/60,000/250,000 gold at 100% success with no protection surcharge, Strike at 5% chance and +5%/+15%/+40% damage, and Basic Punch using item 10281 x25; existing Forgotten Knowledge and Vibrancy scroll repairs remain preserved.
- PR #225 adds the read-only Quest Map Validator on top of the merged World Index and script-resolution contracts: explicit quest source globs produce hashed AID/UID/item/position/teleport/storage evidence, bounded local `.widx` correlation classifies confirmed/map-only/script-only/unresolved/conflicting results, dynamic Lua remains unresolved, static item absence and generic missing positions are treated conservatively, JSON output is atomic, and CI publishes source evidence plus a local correlation toolkit without committing map artifacts.
- PR #219 implements the unified read-only OTBM world index planned by #190 and supersedes conflicted #211: the existing native scanner emits a deterministic binary full-world index, while a memory-mapped Python library and CLI provide bounded item/AID/UID/house-door/teleport/position/region queries with hashes, duplicate detection, atomic output and real-map validation without committing map or index artifacts.
- Raid startup repair in PR #205 splits active Draptor and Yeti monster bursts into progressive waves, enforces spawn-before-stage-advance timing, and separates the Undead Cavebear encounter identity without changing populations, broadcasts, maps or assets.

- The Beginning Carlos repair in PR #157 removes the stage-1 `outfit` completion bypass, gates trade to the accepted stage-6 task, sends tutorial `13` when that trade is first opened, and advances to stage 7 only after an actual meat/ham sale while preserving the existing shop transaction and prices.
- The Beginning Zirella reward-room repair in PR #156 seals UID `50085` until Zirella stage 8 and remaps shovel/rope tutorials to current reward UIDs `50093/50094`; it remains open and requires refresh against the advanced `main` branch.
- GameStore balance sequencing repair in PR #187 emits one post-mutation balance snapshot, removes the parser-level duplicate, and preserves the existing disabled-reason deduplication without changing opcodes, prices or coin arithmetic.
- Achievement trigger repair in PR #184 corrects the exact canonical names for `You Got Horse Power` and `The Professor's Nut`, with a real-source scanner contract and no registry or persistence rename.
- Achievement helper repair in PR #176 replaces sparse-table length/range iteration with a deterministic sorted list of successfully registered IDs, derives real first/last bounds, and fixes public/secret metadata lookup and invalid-input logging without changing registry definitions or player KV data.
- PR #161 extends the OTBM HD pipeline with a one-process batch external AI backend, strict source/output path confinement, independent partial-output normalization, exact source-alpha restoration, compatible override manifests, and an optional no-weights-committed TibiaSR 2x reference adapter.
- PR #154 (replacement for auto-closed #147) adds a deterministic OTBM HD-sprite artifact pipeline: bounded-region sprite export with provenance, padded nearest/external upscale backends, source-alpha restoration, hash/geometry validation, 2x renderer overrides with original-sprite fallback, and visual comparison reports without modifying OTBM or client assets.
- The Beginning Collecting Wood restoration in PR #149 adds exact-position Actions for the five tutorial dead trees and Zirella's cart, creating branch `7772` on the ground and advancing both Zirella storages from stage 6 to 7 after one authentic branch delivery without changing the OTBM.
- PR #145 was closed without merge after historical tutorial MoveEvents were judged broader than the proven current-game contract; no gameplay from that PR landed, while PR #144's evidence classification remains available for semantic review.
- Gameplay Analytics dry-run audit merged in PR #140: a no-server/no-database workflow now tests lifecycle boundaries and maintenance configuration; it fixed false short-session persistence counters, undercounted non-combat discards, and overflowing `LEVEL_BRACKETS` input.
- Gameplay Analytics correctness hardening merged in PR #135: UTC day rollover, combat/death persistence eligibility, retention of short death sessions, truthful dead-letter reporting, rune-charge-aware supply costs, configurable maintenance `LEVEL_BRACKETS`, dimension-safe Grafana series, and focused Lua/Python/shell/MariaDB regression coverage.
- Added persistent multi-agent coordination, autonomous PR/CI/merge rules, active-work discovery, module catalogue, task/handoff templates, ADRs, and cross-repository contracts.

## 2026-07-12 bootstrap inventory

- InstanceManager and InstanceRegionPool are available under `src/game/instance/**`.
- OTBM script-resolution audit tooling is available under `tools/ai-agent/**` with schemas/review rules under `docs/ai-agent/**`.
- Gameplay Analytics and hunt-area tooling are available; production enabling remains an operator decision.
- Account-wide quest access exists with active hardening in PR #124.
- AI overlay materialization and real staging/deployment merged in PRs #125 and #118.
- DI migrations for `SharedPtrManager` and `Scripts` are merged.
- Forge history resolution uses item IDs with name fallback.
