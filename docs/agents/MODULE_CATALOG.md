# Canary Module and System Catalogue

Last reviewed: 2026-07-19

This catalogue makes reusable work visible across agents. Verify current source, tests, and PR state before use.

## Maintenance contract

Update this file in the same PR that adds a reusable module/service/manager/parser/library/tool/workflow abstraction/test utility; changes a public API/config/schema/format/protocol; deprecates or replaces a module; or introduces active work another agent could duplicate.

## Runtime and architecture

| Module/system | Status | Purpose/public surface | Source/tests/docs | Reuse notes |
|---|---|---|---|---|
| InstanceManager | merged (#107, #151, #159, #163, #168, #174, #201, #231, #233) | Strong instance IDs, region-backed lifecycle, stable-ID creature ownership registry, summon inheritance policy, idempotent close, automatic unregister on removal (`Game::removeCreature()`), periodic timeout sweep (`Game::start()`); owned by `Game::getInstanceManager()` | `src/game/instance/instance_id.hpp`, `instance_manager.{hpp,cpp}`, `instance_creature_binder.hpp`, `instance_scoped_event.hpp`, focused tests, `docs/architecture/instance-manager.md` | One manager instance via `Game::getInstanceManager()`; do not create a second registry/manager/binder/global singleton. |
| InstanceRegionPool | merged (#121, #151) | Thread-safe reservation of pre-carved non-overlapping 3D regions | `src/game/instance/instance_region_pool.{hpp,cpp}`, tests, architecture doc | Reuse for physical instance regions; it does not copy maps or own tiles. |
| InstanceArenaService | active (region config + lifecycle merged #289; `/instancearena` talkaction + Lua bindings merged #295; real monster spawn + binder registration + instance-aware summon call site merged #304; real timeout + closing-warning `InstanceScopedEvent` call site + session reaping active) | Administrator-only real consumer of `InstanceManager`: fixed two-region configuration, arena create/activate/close lifecycle with a real 15-minute timeout, per-player enter/leave/close sessions, spawns and registers one real `Cave Rat` per arena (rolled back on any spawn/register failure), a one-shot `InstanceScopedEvent`-guarded closing-warning message, periodic `reapExpiredSessions()` evacuation for timeout-driven closes, `Game.createInstanceArena`/`leaveInstanceArena`/`closeInstanceArena` Lua bindings, `/instancearena create\|leave\|close` talkaction; `Game::getInstanceCreatureBinder()` wires the one production summon-creation call site (`Monster::onThinkDefense()`) to the existing tested ownership-inheritance transaction | `src/game/instance/instance_arena_service.{hpp,cpp}`, `src/game/game.hpp/.cpp` (`getInstanceCreatureBinder()`, periodic sweep call site), `src/creatures/monsters/monster.cpp`, `src/lua/functions/core/game/game_functions.{hpp,cpp}`, `data/scripts/talkactions/gm/instance_arena.lua`, focused tests, `docs/architecture/instanced-test-arena.md`, `docs/agents/programs/INSTANCED_TEST_ARENA_PROGRAM.md` | Reuse for the arena feature only; not a generic dungeon framework. Region coordinates live in exactly one place (`InstanceArenaService::configuredRegions()`); do not duplicate them elsewhere. Player sessions are keyed by stable player id, one active arena per player. Monster spawn/removal and now event scheduling/messaging/evacuation all go through constructor-injected DI seams (`MonsterFactory`/`CreatureRemover`/`DelayedEventScheduler`/`MessageNotifier`/`PlayerEvacuator`; production defaults use real `Monster::createMonster()`/`Game::placeCreature()`/`Game::removeCreature()`/`g_dispatcher()`/`Game::getPlayerByID()`; tests inject synthetic doubles) - do not bypass this with a second ad hoc path. |
| DI service access | merged (#117, #119) | DI resolution for `SharedPtrManager` and `Scripts` | relevant sources/audit/tests | Extend existing DI; do not add raw Meyers singletons. |
| Gameplay Analytics | merged correctness hardening (#135) and dry-run audit (#140); operator enablement remains manual | Runtime telemetry with UTC rollover/combat eligibility, bounded persistence, configurable maintenance brackets, dimension-safe reporting and database-free configuration validation | `data-otservbr-global/scripts/lib/gameplay_analytics_*.lua`, `tools/analytics/**`, `.github/workflows/gameplay-analytics-dry-run.yml`, reporting schema/dashboard, `docs/systems/gameplay-analytics-dry-run.md` | Reuse the live `GameplayAnalytics` wrapper stack; load correctness after reliability, never reload the core, configure brackets through `LEVEL_BRACKETS`, and use `VALIDATE_CONFIG_ONLY=true` before database-backed maintenance. |
| Account-wide quest access | active hardening (#124) | Approved account-shared access, atomic claims, migration/validation/admin paths | `data-otservbr-global/scripts/custom/account_quest_system.lua`, `tools/account-quests/**`, DB/config/API/docs | Use existing APIs/store; do not create a second account quest system. |
| Multi-channel architecture | merged phases; safeguards documented; heartbeat/login-filter/switch-check live (PR #136) + graceful-shutdown OFFLINE publish + live Redis PING startup gate (`CAN-20260713-multichannel-runtime-liveness`) + cross-process DB-row handoff foundation and its first real consumer, the cross-channel mail-loss fix (PR #308, #343) | Shared/per-channel state architecture, session lease/fencing, live channel-runtime heartbeat cache (`ChannelRuntimeRegistry`), fail-closed startup validation incl. real Redis reachability, a general-purpose durable command-inbox/ownership-handoff primitive (`cluster_pending_operations` table, `IClusterPendingOperationRepository`/`IClusterRecordOwnershipResolver`/`ClusterRecordHandoff`, production wrapper `ClusterHandoffRuntime`) for safely transferring responsibility over a persistent record between channel processes without concurrent ownership, stale-owner writes, or lost updates | `src/game/multichannel/**` (notably `cluster_pending_operation_repository.hpp`, `cluster_record_ownership.hpp`, `cluster_record_handoff.{hpp,cpp}`, `cluster_handoff_runtime.hpp`, `mail_delivery_operation_handler.{hpp,cpp}`), `docs/multichannel/*.md` incl. `CROSS_PROCESS_DB_ROW_HANDOFF.md` (the design doc for this mechanism - verify against current source before trusting; `AGENT_HANDOFF.md`'s own P0.4 claim was found stale and corrected on 2026-07-13) | Read latest handoff **and cross-check it against current `main`**, not just at face value - production gates in `docs/multichannel/AGENT_HANDOFF.md` §9 still apply. For any future cross-process ownership-transfer need, reuse `ClusterRecordHandoff`/`ClusterHandoffRuntime` - do not build a second lease/fencing/command-inbox mechanism. House double-ownership fix and DIRTY-session admin tooling both now have a concrete migration plan on top of this foundation (`CROSS_PROCESS_DB_ROW_HANDOFF.md` §12) but remain **not implemented** - do not attempt either without reading that plan first. |
| Forge history ID resolution | merged (#110) | Resolves history item types by ID with name fallback | Forge sources/integration test | Reuse ID-based lookup for custom/duplicate names. |
| Lua filesystem directory helpers | merged (#326) | Shell-free `FileSystem.createDirectory`/`createDirectories` bindings plus compatibility `FS.mkdir`/`FS.mkdir_p` wrappers with boolean/error returns | `src/lua/functions/core/game/global_functions.{hpp,cpp}`, `data/libs/functions/fs.lua`, standalone Lua and C++ binding tests, `docs/lua-api/lua_api.json` | Reuse these methods for Lua directory creation; never reconstruct shell commands or replace the boundary with a character denylist. |
| Forge server authority policies | active (#250) | Validates regular/Convergence Fusion pairs and Transfer class/tier rules before mutation | `src/game/functions/forge_fusion_policy.hpp`, `forge_transfer_policy.hpp`, Player Forge handlers, focused unit/integration tests | Preserve normal Transfer rules from #89; runtime gameplay/E2E and transaction rollback remain separate evidence. |

## AI, map, and deployment tooling

| Module/tool | Status | Purpose/public surface | Source/tests/docs | Reuse notes |
|---|---|---|---|---|
| Real Tibia parity governance | active docs delivery | Repository-wide source precedence, exact provenance, cross-source comparison matrix, proof levels, bounded finding/task lifecycle, CI evidence and durable handoff for every module compared with Real Tibia or donor implementations | `docs/agents/REAL_TIBIA_EVIDENCE_SOURCES.md`, `docs/agents/REAL_TIBIA_PARITY_PLAYBOOK.md`, `docs/agents/programs/REAL_TIBIA_PARITY_PROGRAM.md`, `docs/agents/README.md` | Mandatory for Real Tibia, TibiaWiki/Fandom, CrystalServer, OpenTibiaBR, packet-capture, official-client and donor-map claims. Use module programs plus one bounded task/PR; never rely on chat memory or one universal source ranking. |
| Real Tibia module registry | merged (#324) | Registry-as-code discovery for stable module IDs, categories, source roles, immutable baselines, multidimensional maturity, freshness, dependencies, path/affected-module lookup and deterministic generated indexes | `docs/agents/real-tibia/**`, `tools/agents/real_tibia_registry*.py`, focused tests, `.github/workflows/real-tibia-registry.yml`, ADR | Reuse before any parity task. One JSON-compatible YAML record per module; generated Markdown is read-only; path matches are discovery hints, never ownership or parity proof. |
| Tibia System Decomposition | active TSD-004 (#335, #340, #342, #355, #359) | Bounded logical decomposition over the existing registry; TSD-001 established hierarchy, TSD-002 separated engine/DB/world-persistence boundaries, TSD-003 added account/character/progression boundaries, and TSD-004 adds Bestiary, Bosstiary, Cyclopedia Character and Titles while preserving `cyclopedia`, `charms`, `houses`, `achievements` and `protocol` | `docs/agents/programs/TIBIA_SYSTEM_DECOMPOSITION_PROGRAM.md`, `docs/agents/real-tibia/TIBIA_SYSTEM_DECOMPOSITION_REPORT.md`, `TSD_002A_ENGINE_FOUNDATION_REPORT.md`, `TSD_002B_PERSISTENCE_TRANSACTIONS_REPORT.md`, `TSD_003_ACCOUNT_CHARACTER_PROGRESSION_REPORT.md`, `TSD_004_CYCLOPEDIA_FAMILY_REPORT.md`, 20 bounded decomposition records, focused registry/Upstream Intelligence tests | Complete one feature and lifecycle cycle at a time. Inventory paths/tests do not prove formulas, persistence, protocol, maintained-client behavior, parity, runtime or E2E. Never create a second taxonomy, generator, watcher, parser/renderer, E2E platform or Oteryn registry. |
| Upstream Intelligence and Drift Tracking | merged source-aware foundation (#331, #337) | Read-only bounded monitoring of commits, PRs, issues and releases across OpenTibiaBR Canary/OTClient, CrystalServer, RME and Client Editor; exact source heads, explicit per-source `module_mapping.path_buckets`, source-role-filtered deterministic module discovery, revision-pinned decisions, artifacts and one stable local report issue | `docs/agents/upstream/**`, `docs/agents/programs/UPSTREAM_INTELLIGENCE_PROGRAM.md`, `tools/agents/upstream_intelligence*.py`, focused tests, `.github/workflows/upstream-intelligence.yml`, ADR | Reuse the single source registry and mapper. Missing/unsupported source policy maps no modules; server/client cross-role buckets are excluded; mapping remains a candidate hint and never ownership, parity proof or import authorization. External repositories remain read-only. |
| Wheel of Destiny parity program | active; no current feature owner | Durable Wheel baseline, completed-package history, explicit source matrix and independently testable queue after broad PR #230 delivered only Hunting Task Shop points | `docs/agents/programs/WHEEL_OF_DESTINY_PARITY_PROGRAM.md`, `docs/ai-agent/OTS_AI_WHEEL_OF_DESTINY_VALIDATION.md`, `docs/ai-agent/OTS_AI_WHEEL_OF_DESTINY_VALIDATION_PROJECT.md` | Do not reopen #220/#229/#230 or reuse #279. Revalidate current main and sources, then start only one still-valid queue item such as evidence-backed Blessing critical healing. |
| Wheel of Destiny validation | active draft (#169) | Read-only definition, value, current/legacy protocol-boundary and Hunting Task Shop audits with versioned baseline and machine-readable artifacts | `tools/ai-agent/wheel_*validation.py`, focused tests, `.github/workflows/wheel-of-destiny-validation.yml`, `docs/ai-agent/OTS_AI_WHEEL_OF_DESTINY_VALIDATION_PROJECT.md`, report/runtime plan | Reuse the three audit formats; do not fix gameplay in the audit PR and do not combine confirmed cost, point-accounting, effect, cap, bounds or Task Shop defects into one follow-up PR. |
| Achievement validation audit | comprehensive v2 active (#238); v1 merged (#165) | Full 564-row factual reference catalogue plus conservative per-achievement definition, condition, static handler, persistence/backfill, attainability, runtime/test evidence and status model | `tools/ai-agent/achievement_validation.py`, 13 focused tests, dedicated workflow, `docs/ai-agent/ACHIEVEMENT_*`, project/report docs | Reuse `canary-achievement-audit-v2`; do not create a second parser/scanner. Missing literal calls stay unresolved, reviewed overrides require exact evidence, and wiki presence never authorizes gameplay changes. |
| Achievement point reconciliation | merged (#264) | Recomputes the persisted achievement point aggregate from resolved canonical unlocks after login load and applies five evidence-backed Real Tibia point corrections | `src/creatures/players/components/player_achievement.*`, registry, focused C++ tests, `docs/ai-agent/ACHIEVEMENT_POINT_RECONCILIATION.md` | Reuse the reconciliation API for future point metadata changes. Any unresolved stored canonical name blocks overwrite so historical value is preserved. |
| Weapon Proficiency achievement audit, mastery-state API and awards | merged (#195, #212, #272, #288) | Read-only evidence for achievements 564–567, correct first-entry mastery state, the const mastered-count API, idempotent live/login awards for IDs 564–566 at 1/10/50 mastered weapons, and the exact twelve-item all-mastered condition for secret ID 567 | `tools/ai-agent/weapon_proficiency_*`, `src/creatures/players/components/weapon_proficiency.*`, focused Python/C++ tests and reports | Reuse the count, threshold and exact-set reconciliation paths. ID 567 uses exact item IDs plus normalized mastered state; real-client E2E remains a separate proof layer. Never commit the source asset archive. |
| Cyclopedia validation and remediation | merged (#170, #188, #192) | Read-only validation of Items, Bestiary, Charms, Bosstiary, Map, Character/Titles, Houses and Canary ↔ maintained OTClient surfaces; focused runtime/data regression contracts | `tools/ai-agent/cyclopedia_validation.py`, focused tests, dedicated workflow, `docs/ai-agent/OTS_AI_CYCLOPEDIA_VALIDATION_PROJECT.md`, `CYCLOPEDIA_FIX_LOG.md` | Reuse for Cyclopedia drift checks. Final full scan covered 1,656 active monster files, 749 Bestiary, 249 Bosstiary and 25 Charms with zero findings. Exact shared-form path sets are allowlisted; replacement IDs must remain evidence-backed. |
| Imbuement validation and live-data remediation | merged audit/storage/scroll baseline (#166, #206, #239); live fee/Strike/Punch repair active (#251) | Deterministic audit of Imbuing XML/reference values, scroll/runtime wiring and storage declarations; strict regression for exact Powerful family-to-boss-storage grouping and current live fees/effects/materials | `tools/ai-agent/imbuement_validation.py`, `imbuement_storage_validation.py`, focused tests, `.github/workflows/imbuement-validation.yml`, `docs/ai-agent/IMBUEMENT_VALIDATION_REPORT.md`, `IMBUEMENT_RUNTIME_TEST_PLAN.json` | Reuse for Imbuing drift checks. Nonzero storages must be declared and match the exact `45489..45495` groups. Vibrancy scrolls and confirmed live fee/Strike/Punch values are regression-covered; Featherweight/Vibrancy unlock storages remain evidence-gated rather than guessed. |
| Unified OTBM world index | merged (#219) | Deterministic binary full-world index and bounded query contract for tiles, item placements, AID/UID, house doors, teleport destinations, exact positions and regions | `tools/ai-agent/otbm_item_audit_scan.cpp`, `otbm_world_index*.py`, focused tests/workflow, `docs/ai-agent/OTBM_WORLD_INDEX.md` | Canonical evidence cache for future quest/spawn/NPC/storage/diff validators. Reuse it instead of rescanning or creating another OTBM parser. Generated `.widx` files stay outside Git. |
| Quest Map Validator | merged (#225) | Deterministic source evidence and bounded OTBM correlation for quest AID/UID, item IDs, exact positions, teleport destinations and storage read/write inventory | `tools/ai-agent/quest_map_validation*.py`, focused tests, schema, `.github/workflows/quest-map-validation.yml`, `docs/ai-agent/QUEST_MAP_VALIDATION.md` | Require explicit quest source globs and reuse World Index/script-resolution. Dynamic Lua, storage progression, generic missing positions and item IDs absent from static OTBM remain unresolved rather than guessed. |
| OTBM teleport and reachability validator | merged baseline (#274, #567); executable interaction-aware routing in #580 | Bounded strict/optimistic walkability, indexed teleport validation, reviewed floor-transition edges, route/mechanic reachability, one-way/dead-end/cycle findings and versioned reports; deterministic full predecessor-derived `canary-otbm-e2e-route-plan-v1` export with ordered movement/transition edges, exact transition placement, provenance hashes and fail-closed full-path bounds; optional reviewed interaction-aware mode reruns the same `_bfs()` over a narrower executable edge set, emits `executableDistance` and exact edge-level interaction resolutions, and pins the interaction registry | `tools/ai-agent/otbm_reachability*.py`, `tools/ai-agent/test_otbm_reachability*.py`, focused tests, schemas, `.github/workflows/otbm-reachability.yml`, `docs/ai-agent/OTBM_REACHABILITY.md`, `docs/ai-agent/OTBM_E2E_ROUTE_PLAN.md`, `docs/ai-agent/OTBM_E2E_ROUTE_PLAN.schema.json` | Reachability `_bfs` and its predecessor map remain the canonical and only pathfinder. Plain optimistic walkability is never physical executability. With a reviewed interaction registry, only exact supported conditional crossings and transitions enter the executable graph; unknown appearances, unresolved dynamic/script state and unsupported/ambiguous interactions remain blocked. Static executable resolution is not runtime gameplay proof. |
| OTBM Semantic Landmark Registry | active (#571) | Versioned `canary-otbm-semantic-landmarks-v1` reviewed-name-to-exact-anchor contract with inclusive 3D routing regions, role-specific anchors, exact source-map/World-Index SHA-256 pins and fail-closed deterministic resolution | `tools/ai-agent/otbm_semantic_landmarks.py`, `tools/ai-agent/test_otbm_semantic_landmarks.py`, `docs/ai-agent/OTBM_SEMANTIC_LANDMARKS.schema.json`, `docs/ai-agent/OTBM_SEMANTIC_LANDMARKS.json` | Reuse for semantic route requests. The committed registry starts `unbound` and empty; only evidence-reviewed entries with exact map/index provenance may resolve. Never infer coordinates from memory, sprites, item names, minimap labels or chat history. |
| OTBM Route Interaction Registry | active (#572) | Versioned `canary-otbm-route-interactions-v1` reviewed exact mechanic/transition-to-physical-activation contract with deterministic validation/resolution, narrow selectors, provenance pins and fail-closed Script Resolution gating | `tools/ai-agent/otbm_route_interactions.py`, `tools/ai-agent/test_otbm_route_interactions.py`, `docs/ai-agent/OTBM_ROUTE_INTERACTIONS.md`, schema and unbound registry seed | Reuse as the only reviewed physical interaction semantics registry for OTBM-aware routing. Unknown/ambiguous mechanics and `partially-resolved`/`referenced-only`/`unresolved`/`conflicting` handler evidence remain blocked; static resolution is not gameplay proof. |
| OTBM spawn, boss and NPC validator | merged (#286) | Explicit active-datapack spawn/NPC evidence, conservative creature-definition and literal dynamic-creation inventory, plus bounded World Index and Phase 3 geometry correlation | `tools/ai-agent/otbm_spawn_npc*.py`, focused tests, schemas, `.github/workflows/otbm-spawn-npc-validation.yml`, `docs/ai-agent/OTBM_SPAWN_NPC_VALIDATION.md` | Reuse `canary-otbm-reachability-v1`; keep reward-boss and Bosstiary spawn-boss evidence separate, preserve nonliteral Lua as unresolved, and never auto-repair source or map findings. |
| OTBM storage dependency graph | merged (#299) | Deterministic selected-source storage/KV/database operation inventory and conservative exact prerequisite-to-result graph | `tools/ai-agent/otbm_storage_graph*.py`, focused tests, schema, `.github/workflows/otbm-storage-graph.yml`, `docs/ai-agent/OTBM_STORAGE_GRAPH.md` | Reuse Phase 2 selected-file hashes and optional Phase 3/4 context. Never infer execution order from source proximity; dynamic keys/values remain unresolved and selected-scope missing writers are informational. |
| Semantic OTBM Diff | merged (#311) | Deterministic read-only tile, item-stack, mechanic and Phase 3 walkability comparison over two canonical World Index inputs; report `canary-otbm-semantic-diff-v1` and factual render manifest `canary-otbm-semantic-diff-render-v1` | `tools/ai-agent/otbm_semantic_diff*.py`, 30 focused tests, schema, `.github/workflows/otbm-semantic-diff.yml`, `docs/ai-agent/OTBM_SEMANTIC_DIFF.md` | Reuse the native scanner/World Index, exact Phase 3 tile classifier and existing factual renderer. Do not parse OTBM again, match items heuristically, treat selected-scope correlation as global proof, modify maps or generate AI map imagery. |
| OTBM geometry and consistency audit | merged (#322) | Bounded read-only floor, duplicate-ground, component, house/PZ, invisible-blocker-candidate and reviewed wall/border adjacency evidence; reports `canary-otbm-geometry-audit-v1`, `canary-otbm-geometry-rules-v1` and `canary-otbm-geometry-audit-render-v1` | `tools/ai-agent/otbm_geometry_audit*.py`, 21 focused tests, schemas, `.github/workflows/otbm-geometry-audit.yml`, `docs/ai-agent/OTBM_GEOMETRY_AUDIT.md` | Reuse World Index area postings, the exact Phase 3 classifier and factual renderer. PZ interprets only verified bit `0x0001`; wall/border findings require reviewed rules; never infer visual intent, parse/write OTBM or use AI map imagery. |
| OTBM HD sprite pipeline | merged (#154, #161) | Export bounded-region sprite sets, prepare per-sprite or one-process batch AI output, restore source alpha, validate hashes/geometry, and render 2x override packs with deterministic fallback | `tools/ai-agent/otbm_hd*.py`, focused tests, `docs/ai-agent/OTBM_HD_PIPELINE.md`, `OTBM_HD_BATCH_BACKEND.md` | Renderer-only artifact workflow; does not modify OTBM, client assets, model weights, CWM, or OTClient. Batch backends emit the existing override format. |
| OTBM script-resolution audit | merged (#104) | Read-only mapping of OTBM AID/UID placements to active Lua/XML handlers | `tools/ai-agent/otbm_script_resolution*`, tests, `docs/ai-agent/OTBM_SCRIPT_RESOLUTION*` | Reuse for map mechanic audits; dynamic registrations remain explicit, never guessed. |
| OTBM real-map repair preflight | merged (#406) | Read-only correlation of explicit real-map repair selectors with existing item/mechanic audit placements, native Phase 8 patch anchors and active Lua/XML script resolution; optional review-only `canary-otbm-bounded-patch-plan-v1` drafts; `canary-otbm-repair-preflight-v1` now exposes explicit match/correlation/runtime/patch/review readiness, deterministic appearances/items/rules/script-corpus/Git evidence pins and structural hypothetical resolver before/after diffs | `tools/ai-agent/otbm_repair_preflight*.py`, focused and native-scanner integration tests, `docs/ai-agent/OTBM_REPAIR_PREFLIGHT.md`, report schema | Reuse before any real-map Phase 8 repair. `ok` remains selector-match evidence only; inspect readiness separately. The tool never parses or writes OTBM independently, never executes the patcher, never guesses ambiguous placements, preserves unresolved/conflicting runtime evidence, and does not prove gameplay correctness or player intent. |
| OTBM static map quality gate | active (#419) | Read-only aggregation of compatible Geometry Audit, Reachability and Script Resolution reports for one proven source map into `canary-otbm-map-quality-v1`, with exact `error`/`warning`/`unresolved`/`info` counts, deterministic bounded samples and configurable fail policy | `tools/ai-agent/otbm_map_quality*.py`, focused tests, `docs/ai-agent/OTBM_MAP_QUALITY_GATE.md`, report schema | Reuse existing reports only; never rescan or parse OTBM. Require identical source-map SHA-256 evidence, preserve unresolved runtime state, do not deduplicate component evidence into inferred defects, and never treat a green static gate as gameplay correctness or player intent. |
| OTBM repair sandbox verifier | active (#422) | Applies an existing reviewed Phase 8 fixed-width attribute plan only through the existing bounded patcher to a distinct artifact copy, verifies the Phase 8 evidence manifest, then runs real before/after item audit and script resolution and emits `canary-otbm-repair-sandbox-verification-v1` with exact placement/value/runtime diffs | `tools/ai-agent/otbm_repair_sandbox*.py`, real-scanner integration test, `docs/ai-agent/OTBM_REPAIR_SANDBOX.md`, report schema | Reuse Phase 8 confinement/reparse/World Index/Semantic Diff proof; do not build another writer/parser/diff pipeline. Runtime regression/unresolved/conflict remain review evidence; a green sandbox report does not prove reachability, gameplay correctness, player intent or physical-client E2E. |
| OTBM donor/region merge planner | active (#424) | Deterministic read-only explicit-translation review planning over current/donor canonical World Index regions with `overlay` and `replace-region` actions, exact snapshots, global UID/AID/house-door/teleport conflict evidence and optional script-resolution compatibility; emits `canary-otbm-region-merge-plan-v1` | `tools/ai-agent/otbm_region_merge_planner*.py`, focused tests, `docs/ai-agent/OTBM_REGION_MERGE_PLANNER.md`, report schema | Reuse World Index and Semantic Diff provenance validation. Coordinates and translation must be explicit; `writerReady` is always false. Never write OTBM, heuristically align regions, expand Phase 8, or treat unresolved runtime evidence as handled. |
| OTBM bounded tile-area materializer | active (#426) | Approved structural materialization of complete same-coordinate `OTBM_TILE_AREA` subtrees into a distinct map copy with raw retained-byte confinement, native reparse, selected-area World Index equality and bounded Semantic Diff evidence; contracts `canary-otbm-tile-area-spans-v1`, `canary-otbm-area-materialization-approval-v1`, and `canary-otbm-area-materialization-result-v1` | `tools/ai-agent/otbm_area_materializer*.py`, `otbm_area_materializer_scan.cpp`, real native-scanner integration tests, `docs/ai-agent/OTBM_AREA_MATERIALIZER.md`, schemas, ADR | Reuse the existing native scanner implementation, World Index, Semantic Diff and region planner. v1 requires `replace-region`, zero translation and complete 256x256 tile areas plus a separate SHA-pinned approval; never use it as a full serializer, translated fragment writer, tile-level overlay, Phase 8 expansion or gameplay-correctness proof. |
| OTBM bounded raw tile materializer | active (#467) | Approval-gated replacement of already-existing complete raw `OTBM_TILE`/`OTBM_HOUSETILE` subtrees at exact same absolute positions and canonical parent `TILE_AREA` keys; contracts `canary-otbm-tile-spans-v1`, `canary-otbm-tile-materialization-approval-v1`, and `canary-otbm-tile-materialization-result-v1` prove raw donor equality, exact retained current bytes, native reparse, selected canonical World Index equality and bounded Semantic Diff before create-new publication | `tools/ai-agent/otbm_tile_materializer*.py`, shared `otbm_area_materializer_scan.cpp` wrapper, focused native-scanner integration tests, `docs/ai-agent/OTBM_TILE_MATERIALIZER.md`, schemas, ADR | Reuse the existing scanner wrapper, World Index and Semantic Diff. v1 is replacement-only: both sides must already contain exactly one selected tile with the same position, parent area and node type. Never use it for tile insertion/deletion, translation, tile/house conversion, item-stack editing, arbitrary serialization, in-place writes or gameplay/physical-client proof. |
| OTBM bounded raw tile insertion materializer | active (#482) | Approval-gated insertion of complete donor `OTBM_TILE`/`OTBM_HOUSETILE` raw subtrees at missing exact same absolute positions inside already-existing current canonical parent `TILE_AREA` nodes; contracts `canary-otbm-tile-insertion-approval-v1` and `canary-otbm-tile-insertion-result-v1` prove exact complete-current byte-sequence retention after excluding inserted spans, raw donor equality, native reparse, inserted canonical World Index equality and bounded Semantic Diff before create-new publication | `tools/ai-agent/otbm_tile_insertion_materializer*.py`, existing area/tile span scanners, focused native-scanner integration tests, `docs/ai-agent/OTBM_TILE_INSERTION_MATERIALIZER.md`, schemas, ADR | Reuse existing scanner wrapper, TILE_AREA spans, tile spans, World Index and Semantic Diff. v1 requires the selected tile to be absent in current, unique in donor, and its current parent TILE_AREA to already exist exactly once. Never use it for TILE_AREA creation, replacement/deletion, translation, existing tile/house conversion, item-stack editing, arbitrary serialization, in-place writes or gameplay/physical-client proof. |
| OTBM bounded raw tile deletion materializer | active (#488) | Approval-gated deletion of already-existing complete raw `OTBM_TILE`/`OTBM_HOUSETILE` subtrees at exact reviewed absolute positions; contract `canary-otbm-tile-deletion-approval-v1` pins current raw and canonical tile state and `canary-otbm-tile-deletion-result-v1` proves output is exactly current with only selected spans removed, native reparse, selected raw/World Index absence and bounded Semantic Diff before create-new publication | `tools/ai-agent/otbm_tile_deletion_materializer*.py`, existing tile-span scanner, focused native-scanner integration tests, `docs/ai-agent/OTBM_TILE_DELETION_MATERIALIZER.md`, schemas, ADR | Reuse the existing scanner wrapper, World Index and Semantic Diff. v1 deletes complete selected tile subtrees only and deliberately preserves the parent TILE_AREA even when empty. Never use it for parent-area deletion, insertion/replacement, translation, tile/house conversion, item-stack editing, arbitrary serialization, in-place writes or gameplay/physical-client proof. |
| OTBM bounded raw tile type conversion materializer | active (#498) | Approval-gated conversion of already-existing complete raw `OTBM_TILE` and `OTBM_HOUSETILE` subtrees at exact same absolute positions and canonical parent `TILE_AREA` keys by copying complete reviewed donor subtrees; contracts `canary-otbm-tile-type-conversion-approval-v1` and `canary-otbm-tile-type-conversion-result-v1` prove exact retained current bytes, donor raw equality, native reparse, selected canonical World Index equality and bounded Semantic Diff | `tools/ai-agent/otbm_tile_type_conversion_materializer*.py`, existing tile-span scanner and complete raw tile replacement helpers, focused native-scanner integration tests, `docs/ai-agent/OTBM_TILE_TYPE_CONVERSION_MATERIALIZER.md`, schemas, ADR | Reuse the existing replacement byte writer, scanner, World Index and Semantic Diff. v1 requires both sides to contain exactly one selected tile at the same position and parent area with different node types 5/14. Never patch node type bytes, synthesize house IDs, translate coordinates, insert/delete tiles, edit item stacks, serialize arbitrary nodes, write in place or claim gameplay/physical-client proof. |
| OTBM repair/materialization pipeline | active (#456) | Fail-closed create-new finalization over exactly one existing mutation path: reviewed Phase 8 fixed-width attribute repair through the repair sandbox or approved zero-translation complete-TILE_AREA materialization; replays the pinned mutation into an internal candidate, rebuilds the existing Map Quality Gate from explicit compatible component evidence, requires exact candidate/quality SHA identity, then publishes a byte-identical final map and `canary-otbm-repair-materialization-pipeline-v1` evidence | `tools/ai-agent/otbm_repair_materialization_pipeline*.py`, focused and native-scanner integration tests, `docs/ai-agent/OTBM_REPAIR_MATERIALIZATION_PIPELINE.md`, result schema | Reuse this boundary to finalize reviewed map repairs/materializations. It does not invent reachability evidence, add approval semantics, broaden either writer, deploy/promote maps, or prove gameplay correctness/physical-client behavior. Generated maps, reports and `.widx` files stay outside Git. |
| Promotion overlay materializer | merged (#125) | Atomic, hash-verified materialization of reviewed AI-content overlays | `tools/ai-agent/materialize_promotion_overlay.py` and tests | Feeds deployment pipeline; does not deploy or approve production. |
| Canary staging/deployment pipeline | merged (#118) | Trusted-base assembly, real Canary smoke, atomic switch/rollback, manifest | `tools/deploy/**`, deploy workflows/docs, `docs/systems/ai-content-deployment.md` | Canonical deployment path; extend rather than duplicate. |
| Hunt-area catalogue tooling | merged (#105 and hardening) | Validates/generates verified static hunt rectangles | `tools/analytics/gameplay_analytics_hunt_area*`, fixtures/tests/docs | Never invent coordinates; placeholders are rejected. |

## CI and test infrastructure

| Module/system | Status | Purpose | Paths | Reuse notes |
|---|---|---|---|---|
| OTS Security Validation Platform | active (#433, #440, #451, #462) | Product-neutral source-regression registry and runtime delegation layer with bounded exact-head unauthenticated status-parser and login-protocol boundary packs using fixed code-owned cases, deterministic isolated loopback sources, protocol-aware controls, process/fatal diagnostics and SHA-256 evidence | `tools/security/**`, `tests/security/**`, `docs/security/SECURITY_VALIDATION_PLATFORM.md`, `.github/workflows/security-validation.yml` | Reuse the source-regression, delegation and bounded runtime contracts. Runtime drivers must stay on explicitly authorized disposable targets and reuse Universal E2E/Agent Load lifecycle; never add arbitrary manifest commands, packet payloads, credentials or network targets, and do not treat SEC-003/SEC-004 as successful authentication, game-session, post-login transport or flood coverage. |
| Universal OTS E2E physical gameplay action plans | active (#446, #565, #573) | Strict optional `scenario.steps` plus typed `scenario.assertions.persistence` contracts compiled into one deterministic `scenario-plan.lua` for the controlled real OTClient; phase-one physical actions preserve the canonical safe-logout/relog sentinel; exact `walk_edge` actions synchronize one adjacent same-floor `P0 -> P1` movement by deriving direction from coordinates, issuing one walk request and waiting for the exact destination; directly comparable `player_field` checks (`level`, `experience`) are re-verified through the real client after relog before the second safe logout and through compiled scalar SQL after the full client cycle | `tools/e2e/run_agent_e2e.py`, `tools/e2e/persistence_assertions.py`, `tools/e2e/client/agent_e2e_scenario.lua`, `tests/e2e/test_agent_e2e_scenario_plan.py`, `tests/e2e/test_persistence_assertions.py`, `tests/e2e/test_exact_movement_edges.py`, `tests/e2e/scenarios/platform/action-plan-contract.json`, `tests/e2e/scenarios/movement/physical-movement.json`, `docs/e2e/PHYSICAL_GAMEPLAY_ACTION_PLANS.md` | Reuse the existing Universal Agent E2E MariaDB/server/client lifecycle; do not build another orchestrator. Use `walk_edge` rather than blind direction/count timing for future route-plan movement edges, but keep full route-plan consumption, transitions and interaction semantics in their separate routing packages. Feature-specific expected values remain in scenario manifests and the typed persistence contract exposes no arbitrary SQL. Raw server vocation IDs are intentionally outside the first typed slice because the controlled client uses a different vocation representation; add vocation only with an explicit normalization contract. Concrete movement/combat/item/NPC/monster/storage scenarios must own deterministic fixture evidence rather than invent IDs, coordinates or storage values. Multi-client orchestration and runtime fault injection must extend the canonical runner/workflow in bounded follow-up tasks. |
| Universal Agent Load | active (#393, #444) | Reusable literal-loopback-only Canary status-protocol load/stress runner with bounded JSON profiles, latency/throughput/error metrics, exact-head runtime adapter and a code-owned in-process `RuntimeContext`/`run_runtime` callback seam for bounded consumers after disposable Canary startup; does not claim authenticated gameplay-player capacity | `tools/e2e/run_agent_load.py`, `tools/e2e/run_agent_load_runtime.py`, `tests/e2e/load/**`, `tests/e2e/test_load_runner.py`, `.github/workflows/universal-agent-load.yml` | Reuse this layer for bounded Canary control-plane load evidence and reviewed code-owned local-runtime executors. Never load callback commands, executable paths or target hosts from manifests; keep physical real-OTClient login/relog E2E as the correctness sentinel, never target production or third-party hosts and never disable normal per-IP status throttles. |
| Incremental CI validation and final-head gate | active (#415) | Fail-closed immediate-parent workflow-evidence reuse for proven non-impacting single-commit PR deltas, with profile-specific CI/Load/physical-E2E decisions and exact-final-head full validation through the `ci:final-gate` label | `tools/agents/ci_incremental_validation.py`, focused tests, `.github/workflows/ci.yml`, `.github/workflows/universal-agent-e2e.yml`, `.github/workflows/universal-agent-load.yml`, `AGENTS.md`, `docs/agents/BUILD_TEST_MATRIX.md` | Reuse this helper instead of duplicating path/evidence logic. Missing or non-success parent evidence and workflow/helper changes fail closed. Keep stable Required aggregators current-head; apply `ci:final-gate` before the final commit and make no post-green commit. |
| Gameplay Analytics dry-run audit | merged (#140) | Deterministic Analytics logic/configuration tests without Canary or MariaDB | `.github/workflows/gameplay-analytics-dry-run.yml`, `tools/analytics/test_gameplay_analytics_correctness_edge_cases.lua`, `tools/analytics/test_gameplay_analytics_maintenance_config_dry_run.sh` | Run before staging; it complements rather than replaces real engine/database integration. |
| Required Linux check emission | merged (#132), refined by #415 | Keeps the stable `Required` CI gate emitted while allowing the expensive nested Linux build to be skipped only when incremental scope/evidence says it is not required | `.github/workflows/ci.yml` | Preserve unconditional `Required` aggregation. Selected heavy scopes must succeed; unselected heavy jobs may be skipped only through the fail-closed scope contract. |
| CMake preset matrix | maintained | Canonical configure/build/test entry points | `CMakePresets.json`, CMake, `vcproj/canary.vcxproj` | Update every maintained build entry point for new C++ files. |

## Entry template

```md
### Module name
- Status:
- Responsibility/public surface:
- Source paths:
- Tests:
- Documentation:
- Depends on / used by:
- Task/PR:
- Last verified commit:
```
