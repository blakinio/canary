name: OWA-003D Terminal Reconciliation

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/owa-003d-exact-execution.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  reconcile:
    name: Reconcile exact operational evidence
    runs-on: ubuntu-latest
    steps:
      - name: Checkout owning branch
        uses: actions/checkout@v4
        with:
          ref: feat/CAN-20260731-owa-003d-exact-execution
          fetch-depth: 0

      - name: Reconcile durable programme state
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import re

          root = Path('.')
          operational = root / 'docs/ai-agent/OTBM_TCR_QA_OPERATIONAL_EXECUTION.md'
          operational.write_text(r'''# OWA-003D — Exact TCR-to-QA Operational Execution

## Disposition

```text
EXECUTED_OPERATIONAL_EVIDENCE
OWA003D_RETAINED_TCR_QA_FRESHNESS_IMPACT
```

The exact externally supplied snapshot-B package removed the OWA-003C recovery blocker. Existing merged owners were executed without adding another parser, gateway, router, provenance engine, dependency graph, Semantic Diff implementation, QA engine or renderer.

The first canonical downstream stage remains fail closed:

```text
BLOCKED_EXTERNAL_EVIDENCE
OWA003D_NO_REVIEWED_QA008_ROOT_AND_CANONICAL_MAP_CHANGE_CHAIN
```

This is not a failure of the executed TCR-to-QA chain. It records that client-reference drift is not itself a map change and cannot be converted into a synthetic/no-op Semantic Diff or an OWA-006 candidate.

## Exact external inputs

| Input | Identity |
|---|---|
| Snapshot A archive | version `15.25.bd5a04`; SHA-256 `01c45146e2fcec3f4087844e0cbc1817fb1d60b310a35ac5d88c07aab6f73d1a` |
| Snapshot B archive | version `15.31.69f220`; SHA-256 `95093b15462573cc413fc7752d99ab258f97b58734bc59a8f6ef34cc1921a0f8` |
| Parser revision | `b68fbf7bf26b57f0cf716abffb52cfa951fa66ce` |
| Current source map | SHA-256 `a80de1dda6a9aca3956a9d5b7fb2e0caebb451570d26853fc21beb40d5f31da2` |

Snapshot B exactly matches the package identity recorded by accepted TCR-009 evidence. The source map is the current OWA-001 map and was never treated as a distinct candidate.

## TCR-009 deterministic rematerialization

The historical reviewer-authored manifest path/metadata bytes were not retained, so their old file hashes were not reconstructed from prose. The exact package bytes, selected package inputs and accepted parser revision were instead rerun through the existing manifest and index owners.

The fresh deterministic drift has:

- file SHA-256 `5006bf1cac1b9b0da91c500debedc15270224801abcf5202b01b938d5f691fbb`;
- exactly `27` findings, matching the accepted semantic result;
- `23` added and `4` changed findings;
- component distribution: package metadata `1`, proficiencies `24`, StaticData `2`;
- family distribution: input-component `3`, proficiency definition `23`, StaticData schema-family `1`;
- field-change count `8`.

Fresh final manifests are independently hash-closed as:

- snapshot A: `093be8456aa21e7081208c055443b3b72f4d9afa45b65a90279c5d58f5fc90f9`;
- snapshot B: `e97291b857992414714030136f378e693db8335ee7d51ad3df37ace7ea2a6a9f`.

They are new executions over the exact accepted package identities, not claims that missing historical reviewer-authored bytes were recovered.

## TCR-010 exact evidence gateway

A reviewer-authored binding selected exactly four existing drift fragments: `/findings/0`, `/findings/1`, `/findings/25` and `/findings/26`.

- binding SHA-256: `6db1167b70141798eeaa43590392235adb631cb043d99a426986f32f316dc1b9`;
- gateway file SHA-256: `04fb4447be7e8a263d2808908e94528178447afcbf06fedcf3e4da54c0646776`;
- gateway report SHA-256: `9de775ee19304140c26b7f80e80b477589a8e4f58460fe87649c2f5e60f4d782`;
- evidence-bundle SHA-256: `5245026071cd9af395cd97cc599643fdbdee0bbba033e6843abbda52518ba87f`.

## TCR-011 exact adoption routing

- request SHA-256: `76b4f0da321c795c0b5275664e18ebc40178cd48b341b2e523f0f7705672ec41`;
- routing file SHA-256: `21345ba21a1bad3f0f08e259663cd10abb9101e5a9db1e50e1f63793dbe8600e`;
- routing report SHA-256: `83466a8e8377876e067e535e492ea2998fac9158dfae96585c520451e8cde800`.

The exact reviewed result contains four extracts:

1. package metadata is routed to existing `tcr-client-manifest` / `canary-tibia-client-reference-manifest-v1` ownership;
2. proficiencies are routed to existing `tcr-proficiency-correlation` / `canary-tibia-proficiency-reference-correlation-v1` ownership;
3. StaticData input identity remains targetless `unsupported-fragment-shape`;
4. StaticData schema-family drift remains targetless `unsupported-fragment-shape`.

Unsupported fragments were not silently promoted into handled routes.

## QA-016 exact BOM and release provenance

The existing QA-016 owner was executed over one exact previous/current BOM pair:

- previous canonical BOM SHA-256: `92e82bd868da5b279f36aca4f3429e9831c82892fde7acd0077c2bb075f6dada`;
- current canonical BOM SHA-256: `a1743f9203cac5679b4be7e5b094822d07638b6f95f16e223035f205f0eed6c6`;
- release-provenance file SHA-256: `dab40d9317d262eda7bd5b667d9a480c89f1f64212d0650feebc276acd5f44c0`;
- release-provenance report SHA-256: `e4ed44bc2bb2fa08c232f7d02db1b23143cd75f6610085abbc94b079e7dc7f96`.

The current map and StaticMapData remain current. Client manifest, proficiencies and StaticData are changed and their declared dimensions are stale.

## OWA-003A exact freshness impact

Reviewer-authored mapping:

- review ID: `owa003d.owa003a.review.20260731`;
- review statement: `Exact reviewed mapping binds the routed package-manifest and proficiency-reference extracts to the matching changed QA-016 components and their sole stale dimensions; both unsupported StaticData routes remain targetless and unmapped.`

Exact output identities:

- manifest file SHA-256: `95b5be1dfc130a766ee75473db5b7f113c20cf151bf963e0dc6f8a159d5649c8`;
- manifest canonical SHA-256: `b3a98430c22ce9e7d94742249975907113efd21a24d2e9496544c973f669b7a0`;
- freshness-impact file SHA-256: `6c0334f18cd35524dd85465a0a7d6cf0c8a6e9c959d29c3dbf7352cbb673e241`;
- freshness-impact report SHA-256: `8dbec4bac254a53d4138a50baebce1167993329d50217e9e0d1e9f51250e372c`;
- byte size: `5316`.

The output contains two routed stale dimensions:

- `qa006.tcr-client-manifest`;
- `qa006.tcr-proficiency-reference`.

Both StaticData routes remain explicit targetless `unsupported` impacts. QA-008, QA-002 and QA-007 remain `not-evaluated`; QA-006 remains `not-refreshed`.

## Durable retained artifact

The exact impact and its associated execution identities are retained outside Git:

- workflow run ID: `30614565219`;
- artifact ID: `8786807858`;
- artifact name: `owa-003d-operational-evidence`;
- artifact digest: `sha256:48c79f9ecff88782d4711bb0de7e312d008dca058975123ed4a9a5b55f2d24ea`;
- source branch head: `a68ee8c032415591a07334e663affee930764d35`;
- retention expiry: `2026-10-29T07:56:51Z`.

The downloaded artifact was independently hash-checked. It contains the exact freshness-impact JSON, an execution-metadata JSON binding all upstream report/BOM/provenance/package/map/review identities, and `SHA256SUMS`.

The proprietary archives, selected client inputs, generated manifests, six indexes and full drift/gateway/routing/provenance reports remain outside Git.

## Validation

- `95` focused existing-owner tests pass after reconstructing the expected repository layout from the source artifact;
- relevant Python modules compile;
- drift, gateway, router, QA-016 and freshness execution reruns are byte-deterministic;
- duplicate-key, NaN/Infinity, exact-hash, route-coverage, unsupported-preservation, changed-dependency equality, no-clobber, symlink and input/output-alias protections pass;
- retained artifact SHA-256 checks pass.

## First downstream failure

QA-008 is the first separate canonical owner after the executed impact. It requires a reviewer-declared dependency graph and compatible exact QA-001 and QA-002 reports. No such reviewed root is retained for this client-reference-only change, and no distinct reviewed before/after OTBM change exists from which canonical Semantic Diff and QA-002 evidence could be generated.

Rejected substitutions:

- client-reference drift as map authority;
- current map as both before and candidate;
- a synthetic or no-op Semantic Diff;
- guessed QA-008 dependency edges;
- mapping unsupported StaticData fragments to a convenient owner;
- validators, Physical E2E, QA-007 or QA-006 refresh without their canonical exact inputs.

Re-entry requires, in order:

1. one reviewer-authored QA-008 graph root bound to this exact impact and compatible exact QA-001/QA-002 identities;
2. one real distinct reviewed before/after map-change chain and canonical `canary-otbm-semantic-diff-v1` when map-change regression evidence is required;
3. canonical QA-002 selection, owning validators and selected Universal Physical E2E;
4. QA-007 exact result-set assurance;
5. QA-006 refresh over the resulting exact compatible evidence.

OWA-006 remains independently blocked by `OWA006_NO_RETAINED_REVIEWED_REAL_CANDIDATE_CHAIN`.
''', encoding='utf-8')

          executed = root / 'docs/ai-agent/OTBM_TCR_QA_EXECUTED_EVIDENCE.md'
          text = executed.read_text(encoding='utf-8')
          marker = '# OWA-003C — Executed TCR-to-QA Evidence Recovery\n'
          if text.count(marker) != 1:
              raise SystemExit('unexpected executed-evidence title count')
          superseding = r'''

## Superseding operational status — OWA-003D

The exact snapshot-B package was supplied on 2026-07-31 and matches version `15.31.69f220` with archive SHA-256 `95093b15462573cc413fc7752d99ab258f97b58734bc59a8f6ef34cc1921a0f8`. OWA-003D therefore supersedes the OWA-003C missing-payload blocker for this execution runtime.

Existing TCR-009/010/011, QA-016 and OWA-003A owners produced and retained one real operational impact in workflow run `30614565219`, artifact `8786807858`. Exact identities, review statement, validation and the new downstream fail-closed boundary are recorded in [OTBM_TCR_QA_OPERATIONAL_EXECUTION.md](OTBM_TCR_QA_OPERATIONAL_EXECUTION.md).

The remaining blocker is no longer missing snapshot B. It is `OWA003D_NO_REVIEWED_QA008_ROOT_AND_CANONICAL_MAP_CHANGE_CHAIN`. The OWA-003C record below is preserved as historical recovery evidence.
'''
          text = text.replace(marker, marker + superseding, 1)
          text = text.replace('## Disposition\n', '## Historical OWA-003C disposition\n', 1)
          executed.write_text(text, encoding='utf-8')

          programme = root / 'docs/agents/programs/OTBM_WORLD_ASSURANCE_OPERATIONS_PROGRAM.md'
          text = programme.read_text(encoding='utf-8')
          text = re.sub(r'^updated: .*$', 'updated: 2026-07-31T10:00:00+02:00', text, count=1, flags=re.M)
          text = re.sub(r'^last_verified_commit: ".*"$', 'last_verified_commit: "a68ee8c032415591a07334e663affee930764d35"', text, count=1, flags=re.M)
          text, n = re.subn(
              r'(?s)# Active tasks\n\n.*?(?=\n# Queue)',
              '# Active tasks\n\nOWA-003D exact operational execution is active in draft PR #1044. The exact snapshot-B package removed the OWA-003C recovery blocker; TCR-009/010/011, QA-016 and OWA-003A have executed and a real freshness-impact artifact is retained. The package is terminally validating the first downstream boundary without inventing a map change.\n\nOWA-006 remains independently blocked by `OWA006_NO_RETAINED_REVIEWED_REAL_CANDIDATE_CHAIN`; the current map is not a candidate.',
              text,
              count=1,
          )
          if n != 1:
              raise SystemExit('active tasks section not replaced')
          old_row_re = r'^\| OWA-003 \|.*$'
          new_row = '| OWA-003 | TCR-to-QA Drift and Freshness Integration | OWA-003D exact operational impact retained; downstream blocked — `OWA003D_NO_REVIEWED_QA008_ROOT_AND_CANONICAL_MAP_CHANGE_CHAIN` | retained run `30614565219` / artifact `8786807858`; reviewer-authored QA-008 root plus compatible canonical map-change evidence for downstream | medium | Preserve the executed impact and re-enter QA-008/Semantic Diff/QA-002 only with their exact canonical inputs; never convert client drift into a synthetic map change. |'
          text, n = re.subn(old_row_re, new_row, text, count=1, flags=re.M)
          if n != 1:
              raise SystemExit('OWA-003 queue row not replaced')
          seq_anchor = '8. **OWA-003C** — exact-evidence recovery merged via #1035 and lifecycle-closed via #1036. It exhausted retained Actions artifacts, official-launcher rematerialization and the public exact-tag mirror, then stopped at `OWA003C_NO_RECOVERABLE_EXACT_TCR009_SNAPSHOT_B_PAYLOAD_OR_RETAINED_REPORT_CHAIN` without reconstructing report bytes from hashes or summaries.'
          if text.count(seq_anchor) != 1:
              raise SystemExit('sequence anchor missing')
          text = text.replace(seq_anchor, seq_anchor + '\n9. **OWA-003D** — exact snapshot B supplied; existing TCR-009/010/011, QA-016 and OWA-003A owners executed; workflow run `30614565219`, artifact `8786807858` retains the exact impact. Downstream stops before QA-008 at `OWA003D_NO_REVIEWED_QA008_ROOT_AND_CANONICAL_MAP_CHANGE_CHAIN` because no reviewed dependency root or distinct canonical map-change chain exists.', 1)
          old_terminal = 'All currently executable OWA recovery work has been performed. OWA-006 remains independently blocked on a real candidate chain. OWA-003 downstream remains blocked before TCR-010 because exact recoverable snapshot-B/report-chain bytes are absent; QA-008/002/007/006 remain unevaluated.'
          new_terminal = 'The previously missing exact snapshot-B input has now been supplied and the executable TCR-to-QA chain has been performed. OWA-003 downstream remains fail-closed before QA-008 because no reviewer-authored graph root and no distinct canonical map-change chain exist; QA-008/002/007/006 remain unevaluated/not-refreshed. OWA-006 remains independently blocked on a real candidate chain.'
          if text.count(old_terminal) != 1:
              raise SystemExit('terminal paragraph missing')
          text = text.replace(old_terminal, new_terminal, 1)
          insert_before = '\n## OWA-004 — Runtime Incident to OTBM Evidence Bridge'
          if text.count(insert_before) != 1:
              raise SystemExit('OWA-004 section anchor missing')
          section = r'''

### OWA-003D exact operational execution

Draft PR #1044 uses the exact externally supplied snapshot-B archive (`15.31.69f220`, SHA-256 `95093b15462573cc413fc7752d99ab258f97b58734bc59a8f6ef34cc1921a0f8`) and snapshot A through existing merged owners. A fresh deterministic TCR-009 drift reproduces the accepted 27-finding semantic result. TCR-010 selected four exact reviewed fragments; TCR-011 routed package metadata and proficiencies to existing owners while preserving two StaticData fragments as targetless unsupported routes. QA-016 produced one exact previous/current BOM comparison and OWA-003A emitted two mapped stale dimensions.

The exact freshness impact is retained by workflow run `30614565219`, artifact `8786807858`, digest `sha256:48c79f9ecff88782d4711bb0de7e312d008dca058975123ed4a9a5b55f2d24ea`. Its report SHA-256 is `8dbec4bac254a53d4138a50baebce1167993329d50217e9e0d1e9f51250e372c`; the reviewer-authored mapping statement and all associated gateway/routing/provenance/BOM identities are retained in execution metadata.

QA-008 is the next canonical owner, but it requires a reviewed dependency graph plus compatible exact QA-001/QA-002 evidence. No distinct reviewed OTBM before/after chain exists, and client-reference drift is not map authority. Therefore Semantic Diff, QA-002, validators, Physical E2E, QA-007 and QA-006 refresh remain unevaluated/not-refreshed under `OWA003D_NO_REVIEWED_QA008_ROOT_AND_CANONICAL_MAP_CHANGE_CHAIN`. No current-map-as-candidate or no-op diff substitution is accepted.
'''
          text = text.replace(insert_before, section + insert_before, 1)
          programme.write_text(text, encoding='utf-8')

          roadmap = root / 'docs/ai-agent/OTBM_WORLD_ASSURANCE_OPERATIONS_ROADMAP.md'
          text = roadmap.read_text(encoding='utf-8')
          old_priority = '**Priority: OWA-003A stable/merged; OWA-003B lifecycle-closed; OWA-003C recovery/lifecycle closed, blocked external evidence.**'
          new_priority = '**Priority: OWA-003D exact operational impact retained; downstream QA-008/map-change chain blocked external evidence.**'
          if text.count(old_priority) != 1:
              raise SystemExit('roadmap priority missing')
          text = text.replace(old_priority, new_priority, 1)
          hard_anchor = '\n## Hard boundaries'
          if text.count(hard_anchor) != 1:
              raise SystemExit('roadmap hard-boundary anchor missing')
          roadmap_section = r'''

## OWA-003D executed operational evidence — draft PR #1044

```text
EXECUTED_OPERATIONAL_EVIDENCE
OWA003D_RETAINED_TCR_QA_FRESHNESS_IMPACT
```

Exact snapshot B version `15.31.69f220` and archive SHA-256 `95093b15462573cc413fc7752d99ab258f97b58734bc59a8f6ef34cc1921a0f8` were supplied. Existing TCR-009/010/011, QA-016 and OWA-003A owners executed without adding parallel infrastructure.

The retained impact is bound to workflow run `30614565219`, artifact `8786807858`, artifact digest `sha256:48c79f9ecff88782d4711bb0de7e312d008dca058975123ed4a9a5b55f2d24ea`, impact file SHA-256 `6c0334f18cd35524dd85465a0a7d6cf0c8a6e9c959d29c3dbf7352cbb673e241` and report SHA-256 `8dbec4bac254a53d4138a50baebce1167993329d50217e9e0d1e9f51250e372c`.

Two exact routed dimensions are stale: `qa006.tcr-client-manifest` and `qa006.tcr-proficiency-reference`. Two StaticData routes remain targetless unsupported outcomes.

The next boundary is:

```text
BLOCKED_EXTERNAL_EVIDENCE
OWA003D_NO_REVIEWED_QA008_ROOT_AND_CANONICAL_MAP_CHANGE_CHAIN
```

QA-008 needs a reviewer-declared graph and compatible QA-001/QA-002 evidence. No distinct reviewed map candidate/change chain exists; client drift is not a Semantic OTBM Diff. QA-008, Semantic Diff, QA-002, validators, Physical E2E, QA-007 and QA-006 refresh therefore remain separate and unevaluated/not-refreshed.
'''
          text = text.replace(hard_anchor, roadmap_section + hard_anchor, 1)
          roadmap.write_text(text, encoding='utf-8')

          task = root / 'docs/agents/tasks/active/CAN-20260731-owa-003d-exact-execution.md'
          prefix = r'''---
task_id: CAN-20260731-owa-003d-exact-execution
program_id: CAN-PROGRAM-OTBM-WORLD-ASSURANCE-OPERATIONS
coordination_id: OWA-003D
status: blocked
agent: "GPT-5.6 Thinking"
branch: feat/CAN-20260731-owa-003d-exact-execution
base_branch: main
created: 2026-07-31T09:15:00+02:00
updated: 2026-07-31T10:00:00+02:00
last_verified_commit: "a68ee8c032415591a07334e663affee930764d35"
risk: high
related_issue: ""
related_pr: "1044"
depends_on:
  - TCR-009 merged stable client-reference drift producer
  - TCR-010 merged stable evidence gateway
  - TCR-011 merged stable adoption router
  - OWA-003A merged stable TCR-to-QA freshness integration
  - exact snapshot A package version 15.25.bd5a04 supplied outside Git
  - exact snapshot B package version 15.31.69f220 supplied outside Git
blocks:
  - OWA-003 downstream QA-008/002/007/006 assurance
owned_paths:
  exclusive:
    - docs/agents/tasks/active/CAN-20260731-owa-003d-exact-execution.md
    - docs/ai-agent/OTBM_TCR_QA_EXECUTED_EVIDENCE.md
    - docs/ai-agent/OTBM_TCR_QA_OPERATIONAL_EXECUTION.md
  shared:
    - docs/agents/programs/OTBM_WORLD_ASSURANCE_OPERATIONS_PROGRAM.md
    - docs/ai-agent/OTBM_WORLD_ASSURANCE_OPERATIONS_ROADMAP.md
  read_only:
    - exact user-supplied client packages outside Git
    - tools/ai-agent/tibia_client_reference_*.py
    - tools/ai-agent/tibia_reference_adoption_*.py
    - tools/ai-agent/otbm_tcr_qa_freshness*.py
    - docs/ai-agent/TIBIA_CLIENT_REFERENCE_*.schema.json
    - docs/ai-agent/TIBIA_REFERENCE_ADOPTION_*.schema.json
    - docs/ai-agent/OTBM_TCR_QA_FRESHNESS*.schema.json
modules_touched:
  - Tibia client-reference operational evidence
  - OTBM TCR-to-QA freshness operational evidence
reuses:
  - canary-tibia-client-reference-manifest-v1
  - canary-tibia-staticdata-index-v1
  - canary-tibia-staticmapdata-index-v1
  - canary-tibia-proficiency-index-v1
  - canary-tibia-client-reference-drift-v1
  - canary-tibia-client-reference-evidence-gateway-v1
  - canary-tibia-reference-adoption-routing-v1
  - canary-otbm-release-provenance-v1
  - canary-otbm-tcr-qa-freshness-impact-v1
public_interfaces: []
cross_repo_tasks: []
---

# Goal

Use the exact externally supplied snapshot-B package to deterministically rematerialize the accepted TCR-009 evidence chain, execute the existing TCR-010 and TCR-011 owners, and produce one real retained OWA-003A freshness-impact artifact without committing proprietary client payloads or replacing any existing parser, router, provenance owner or QA engine.

# Acceptance criteria

- [x] Verify snapshot A and B package identities, versions and bounded package contents.
- [x] Rematerialize exact package-bound manifests and all six generated indexes with parser revision `b68fbf7bf26b57f0cf716abffb52cfa951fa66ce`; preserve the missing historical reviewer-authored manifest bytes as not recovered.
- [x] Reproduce the accepted deterministic 27-finding TCR-009 semantic result without guessing historical payload bytes.
- [x] Execute one exact reviewed TCR-010 evidence gateway report.
- [x] Execute one exact reviewed TCR-011 adoption-routing report.
- [x] Resolve compatible current/previous QA-016 BOM and release-provenance evidence.
- [x] Execute OWA-003A with a reviewer-authored exact mapping and retain file/report/manifest/routing/provenance/BOM identities.
- [x] Keep proprietary packages and generated client indexes outside Git; commit only non-proprietary metadata and exact durable references.
- [x] Evaluate the first downstream OWA-003 evidence requirement without synthesizing a map change.
- [ ] Pass exact-head checks, squash-merge, then complete lifecycle in a separate PR.

## Context checkpoint

```yaml
checkpoint_version: 1
updated_at: 2026-07-31T10:00:00+02:00
head: a68ee8c032415591a07334e663affee930764d35
branch: feat/CAN-20260731-owa-003d-exact-execution
pr: 1044
status: blocked
context_routes:
  - agent-governance
  - otbm
  - real-tibia-parity
  - testing
owned_paths:
  - docs/agents/tasks/active/CAN-20260731-owa-003d-exact-execution.md
  - docs/ai-agent/OTBM_TCR_QA_EXECUTED_EVIDENCE.md
  - docs/ai-agent/OTBM_TCR_QA_OPERATIONAL_EXECUTION.md
  - docs/agents/programs/OTBM_WORLD_ASSURANCE_OPERATIONS_PROGRAM.md
  - docs/ai-agent/OTBM_WORLD_ASSURANCE_OPERATIONS_ROADMAP.md
proven:
  - Snapshot B SHA-256 95093b15462573cc413fc7752d99ab258f97b58734bc59a8f6ef34cc1921a0f8 and version 15.31.69f220 exactly match accepted TCR-009 identity.
  - Existing owners reproduced the accepted 27-finding semantic TCR-009 result from exact snapshot A/B package bytes and parser revision b68fbf7bf26b57f0cf716abffb52cfa951fa66ce.
  - TCR-010 selected four exact reviewed fragments and TCR-011 routed two to existing owners while preserving two targetless unsupported StaticData routes.
  - QA-016 exact BOM/provenance comparison and reviewer-authored OWA-003A mapping produced two exact stale dimensions.
  - Workflow run 30614565219 artifact 8786807858 retains the exact freshness impact and associated execution identities for 90 days.
  - Ninety-five focused existing-owner tests pass and all operational producer reruns are deterministic.
  - No proprietary archive, selected client input, generated index, map, runtime state or database state was committed or mutated.
derived:
  - OWA003C_NO_RECOVERABLE_EXACT_TCR009_SNAPSHOT_B_PAYLOAD_OR_RETAINED_REPORT_CHAIN is superseded for this execution.
  - Client-reference drift cannot be promoted into map authority, a no-op Semantic Diff or an OWA-006 candidate.
unknown:
  - A reviewer-authored QA-008 graph root compatible with this exact impact and QA-001/QA-002.
  - One distinct reviewed real before/after OTBM change chain for canonical Semantic Diff and downstream regression evidence.
conflicts: []
first_failure:
  marker: OWA003D_NO_REVIEWED_QA008_ROOT_AND_CANONICAL_MAP_CHANGE_CHAIN
  evidence: QA-008 requires a reviewed graph and compatible QA-001/QA-002; no distinct reviewed map change exists, so Semantic Diff and later owners cannot execute without fabrication.
rejected_hypotheses:
  - Commit proprietary package bytes or full generated client-reference reports.
  - Replace existing parser, drift producer, gateway, router, provenance or freshness owners.
  - Reconstruct historical report bytes from hashes or task prose.
  - Guess route, component, dimension or dependency-edge mappings.
  - Treat current map as both before and candidate or generate a no-op Semantic Diff.
  - Promote unsupported StaticData routes into handled evidence.
changed_paths:
  - docs/agents/programs/OTBM_WORLD_ASSURANCE_OPERATIONS_PROGRAM.md
  - docs/agents/tasks/active/CAN-20260731-owa-003d-exact-execution.md
  - docs/ai-agent/OTBM_TCR_QA_EXECUTED_EVIDENCE.md
  - docs/ai-agent/OTBM_TCR_QA_OPERATIONAL_EXECUTION.md
  - docs/ai-agent/OTBM_WORLD_ASSURANCE_OPERATIONS_ROADMAP.md
validation:
  - command: exact snapshot SHA-256/version and selected-input closure
    result: PASS
    evidence: Both archives and all eight selected package inputs match declared exact identities.
  - command: existing TCR-009/010/011, QA-016 and OWA-003A owner execution plus deterministic rerun
    result: PASS
    evidence: All before/after output hashes are identical; TCR-009 contains exactly 27 findings.
  - command: focused existing-owner tests
    result: PASS
    evidence: 95 tests pass, including malformed input, no-clobber, exact coverage and output-safety cases.
  - command: retained Actions artifact verification
    result: PASS
    evidence: Run 30614565219 artifact 8786807858 digest and embedded SHA256SUMS independently verify.
  - command: downstream QA-008 / Semantic Diff preflight
    result: BLOCKED
    evidence: No reviewed QA-008 root and no distinct canonical map-change chain exist; later stages remain unevaluated/not-refreshed.
blockers:
  - OWA003D_NO_REVIEWED_QA008_ROOT_AND_CANONICAL_MAP_CHANGE_CHAIN
next_action: Remove the temporary workflow, pass exact-head feature gates, merge PR 1044 and archive the blocked terminal lifecycle in a separate PR.
```
'''
          task.write_text(prefix, encoding='utf-8')
          PY

          git rm .github/workflows/owa-003d-exact-execution.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add docs/agents/programs/OTBM_WORLD_ASSURANCE_OPERATIONS_PROGRAM.md \
                  docs/agents/tasks/active/CAN-20260731-owa-003d-exact-execution.md \
                  docs/ai-agent/OTBM_TCR_QA_EXECUTED_EVIDENCE.md \
                  docs/ai-agent/OTBM_TCR_QA_OPERATIONAL_EXECUTION.md \
                  docs/ai-agent/OTBM_WORLD_ASSURANCE_OPERATIONS_ROADMAP.md
          git diff --cached --check
          git commit -m "docs(otbm): retain exact OWA-003 operational evidence"
          git push origin HEAD:feat/CAN-20260731-owa-003d-exact-execution
