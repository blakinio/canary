#!/usr/bin/env python3
from __future__ import annotations

from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]

def replace_once(path: str, old: str, new: str) -> None:
    target = ROOT / path
    text = target.read_text(encoding='utf-8')
    count = text.count(old)
    if count != 1:
        raise RuntimeError(f'{path}: expected exactly one anchor, found {count}')
    target.write_text(text.replace(old, new, 1), encoding='utf-8')

def create_exact(path: str, content: str) -> None:
    target = ROOT / path
    if target.exists():
        raise RuntimeError(f'{path}: refusing to overwrite existing file')
    target.parent.mkdir(parents=True, exist_ok=True)
    target.write_text(content, encoding='utf-8')

def main() -> None:
    replace_once('src/creatures/combat/condition.cpp', '\tinternalLightTicks = 0;\n\tlightChangeInterval = ticks / lightInfo.level;\n\tcreature->setCreatureLight(lightInfo);\n', '\tinternalLightTicks = 0;\n\tlightInfo.level = std::max<uint8_t>(1, lightInfo.level);\n\tlightChangeInterval = ticks / lightInfo.level;\n\tcreature->setCreatureLight(lightInfo);\n')
    replace_once('src/creatures/combat/condition.cpp', '\t\tlightInfo.level = value;\n\t\treturn true;\n\t} else if (attr == CONDITIONATTR_LIGHTTICKS) {\n', '\t\tlightInfo.level = std::max<uint32_t>(1, value);\n\t\treturn true;\n\t} else if (attr == CONDITIONATTR_LIGHTTICKS) {\n')
    replace_once('tests/unit/players/condition/CMakeLists.txt', 'target_sources(\n    canary_ut\n    PRIVATE player_paralyze_walk_exhaust_test.cpp\n)\n', 'target_sources(\n    canary_ut\n    PRIVATE condition_light_test.cpp\n            player_paralyze_walk_exhaust_test.cpp\n)\n')
    replace_once('docs/agents/programs/CRYSTALSERVER_COMPARISON_PROGRAM.md', 'updated: 2026-07-13T21:21:00Z\nlast_verified_commit: "bceccba9349d35a1d84f446757e53ac3adb602e1"\n', 'updated: 2026-07-14T00:05:00+02:00\nlast_verified_commit: "b2036bd5d56423894b72eaa2ebaff32feba382a5"\n')
    replace_once('docs/agents/programs/CRYSTALSERVER_COMPARISON_PROGRAM.md', '# Active tasks\n\nNone. Select exactly one bounded queue item and create a new task before implementation.\n', '# Active tasks\n\n| Task/PR | Candidate | Status | Scope |\n|---|---|---|---|\n| `CAN-20260713-conditionlight-zero-level` / [#297](https://github.com/blakinio/canary/pull/297) | `CS-001` | active | Test-first normalization of zero light levels at `ConditionLight` start and deserialization boundaries. |\n')
    replace_once('docs/agents/programs/CRYSTALSERVER_COMPARISON_PROGRAM.md', '| `CS-001` | `a7350014528002fb27ed64d260a96d28a580d41a` | `VALID_FIX_MISSING` | high | `ConditionLight` zero-level division/deserialization | Create a separate test-first task after fresh baseline, ownership, and test-harness checks. |\n', '| `CS-001` | `a7350014528002fb27ed64d260a96d28a580d41a` | `VALID_FIX_MISSING` | high | `ConditionLight` zero-level division/deserialization | Active in task `CAN-20260713-conditionlight-zero-level` / PR #297; merge only after focused C++ and required CI gates pass. |\n')
    replace_once('docs/agents/CHANGELOG.md', '## Unreleased\n\n', '## Unreleased\n\n- PR #297 normalizes zero-level `ConditionLight` state at construction/start and deserialization boundaries, preventing integer division by zero while preserving valid light levels and fade intervals.\n\n')
    replace_once('docs/agents/tasks/active/CAN-20260713-conditionlight-zero-level.md', 'updated: 2026-07-14T00:02:00+02:00', 'updated: 2026-07-14T00:05:00+02:00')
    replace_once('docs/agents/tasks/active/CAN-20260713-conditionlight-zero-level.md', '- [ ] A focused regression test proves zero-level deserialization is normalized to `1`.\n- [ ] A focused regression test proves starting a zero-level light condition does not divide by zero and produces a valid level and interval.\n- [ ] Valid nonzero light levels preserve their existing interval and serialization behavior.\n- [ ] The implementation is limited to the missing `ConditionLight` input boundaries.\n- [ ] Temporary patch workflow/script remove themselves from the final implementation diff.\n', '- [x] A focused regression test proves zero-level deserialization is normalized to `1`.\n- [x] A focused regression test proves starting a zero-level light condition does not divide by zero and produces a valid level and interval.\n- [x] Valid nonzero light levels preserve their existing interval and serialization behavior.\n- [x] The implementation is limited to the missing `ConditionLight` input boundaries.\n- [x] Temporary patch workflow/script remove themselves from the final implementation diff.\n')
    replace_once('docs/agents/tasks/active/CAN-20260713-conditionlight-zero-level.md', '| `src/creatures/combat/condition.cpp` | exclusive | normalize start level and deserialized level | planned |\n| `tests/unit/players/condition/condition_light_test.cpp` | exclusive | focused regression tests | planned |\n| `tests/unit/players/condition/CMakeLists.txt` | exclusive | register test source | planned |\n| temporary patch workflow/script | exclusive | exact-anchor branch mutation, then self-removal | temporary |\n| comparison program | shared | active task/provenance then completion | planned |\n| changelog | shared | behavior-level completion note | planned |\n', '| `src/creatures/combat/condition.cpp` | exclusive | normalize start level and deserialized level | implemented |\n| `tests/unit/players/condition/condition_light_test.cpp` | exclusive | focused regression tests | implemented |\n| `tests/unit/players/condition/CMakeLists.txt` | exclusive | register test source | implemented |\n| temporary patch workflow/script | exclusive | exact-anchor branch mutation, then self-removal | removed in implementation commit |\n| comparison program | shared | active task and provenance | updated |\n| changelog | shared | behavior-level completion note | updated |\n')
    replace_once('docs/agents/tasks/active/CAN-20260713-conditionlight-zero-level.md', '# Remaining work\n\n1. Run the bounded patch workflow, then validate its final diff and CI.\n', '## 2026-07-14T00:05:00+02:00\n\n- Changed: normalized zero light levels at both missing runtime boundaries; added three focused serialization/runtime tests; registered the test; updated program and changelog.\n- Learned: existing serialization provides sufficient observation of private light state, so no public test-only API is needed.\n- Failed/blocked: local execution remains unavailable; GitHub CI must compile and execute the new tests.\n- Result: implementation is complete on the branch; temporary patch files are removed from the generated commit.\n\n# Remaining work\n\n1. Inspect the generated diff and current-head CI; repair any compile/test failure from logs.\n2. Complete the autonomous merge gate and archive the task after merge.\n')
    create_exact('tests/unit/players/condition/condition_light_test.cpp', '/**\n * Canary - A free and open-source MMORPG server emulator\n * Copyright (©) 2019–present OpenTibiaBR <opentibiabr@outlook.com>\n * Repository: https://github.com/opentibiabr/canary\n * License: https://github.com/opentibiabr/canary/blob/main/LICENSE\n * Contributors: https://github.com/opentibiabr/canary/graphs/contributors\n * Website: https://docs.opentibiabr.com/\n */\n\n#include "creatures/combat/condition.hpp"\n#include "creatures/players/player.hpp"\n#include "io/fileloader.hpp"\n#include "items/tile.hpp"\n#include "utils/tools.hpp"\n\nnamespace {\n\tstruct SerializedLightState {\n\t\tuint32_t level = 0;\n\t\tuint32_t interval = 0;\n\t};\n\n\ttemplate <typename T>\n\tbool readAttribute(PropStream &stream, ConditionAttr_t expected, T &value) {\n\t\tuint8_t attribute = 0;\n\t\treturn stream.read<uint8_t>(attribute)\n\t\t\t&& attribute == static_cast<uint8_t>(expected)\n\t\t\t&& stream.read<T>(value);\n\t}\n\n\tstd::optional<SerializedLightState> serializeLightState(ConditionLight &condition) {\n\t\tPropWriteStream output;\n\t\tcondition.serialize(output);\n\n\t\tsize_t size = 0;\n\t\tconst char* data = output.getStream(size);\n\t\tPropStream input;\n\t\tinput.init(data, size);\n\n\t\tint8_t type = 0;\n\t\tuint32_t id = 0;\n\t\tuint32_t ticks = 0;\n\t\tuint8_t isBuff = 0;\n\t\tuint32_t subId = 0;\n\t\tuint16_t tickSound = 0;\n\t\tuint16_t addSound = 0;\n\t\tbool persistent = false;\n\t\tuint32_t color = 0;\n\t\tSerializedLightState state;\n\t\tuint32_t internalTicks = 0;\n\n\t\tif (!readAttribute(input, CONDITIONATTR_TYPE, type)\n\t\t\t|| !readAttribute(input, CONDITIONATTR_ID, id)\n\t\t\t|| !readAttribute(input, CONDITIONATTR_TICKS, ticks)\n\t\t\t|| !readAttribute(input, CONDITIONATTR_ISBUFF, isBuff)\n\t\t\t|| !readAttribute(input, CONDITIONATTR_SUBID, subId)\n\t\t\t|| !readAttribute(input, CONDITIONATTR_TICKSOUND, tickSound)\n\t\t\t|| !readAttribute(input, CONDITIONATTR_ADDSOUND, addSound)\n\t\t\t|| !readAttribute(input, CONDITIONATTR_PERSISTENT, persistent)\n\t\t\t|| !readAttribute(input, CONDITIONATTR_LIGHTCOLOR, color)\n\t\t\t|| !readAttribute(input, CONDITIONATTR_LIGHTLEVEL, state.level)\n\t\t\t|| !readAttribute(input, CONDITIONATTR_LIGHTTICKS, internalTicks)\n\t\t\t|| !readAttribute(input, CONDITIONATTR_LIGHTINTERVAL, state.interval)\n\t\t\t|| input.size() != 0) {\n\t\t\treturn std::nullopt;\n\t\t}\n\n\t\treturn state;\n\t}\n\n\tstd::shared_ptr<Player> createPlayerAt(const std::shared_ptr<Tile> &tile) {\n\t\tauto player = std::make_shared<Player>();\n\t\tplayer->setParent(tile);\n\t\treturn player;\n\t}\n}\n\nTEST(ConditionLightTest, NormalizesZeroLevelDuringDeserialization) {\n\tConditionLight condition(CONDITIONID_DEFAULT, CONDITION_LIGHT, 5000, false, 0, 7, 215);\n\n\tPropWriteStream output;\n\toutput.write<uint32_t>(0);\n\n\tsize_t size = 0;\n\tconst char* data = output.getStream(size);\n\tPropStream input;\n\tinput.init(data, size);\n\n\tASSERT_TRUE(condition.unserializeProp(CONDITIONATTR_LIGHTLEVEL, input));\n\tASSERT_EQ(0u, input.size());\n\n\tconst auto state = serializeLightState(condition);\n\tASSERT_TRUE(state.has_value());\n\tEXPECT_EQ(1u, state->level);\n}\n\nTEST(ConditionLightTest, StartsZeroLevelAtMinimumValidLevelAndInterval) {\n\tUPDATE_OTSYS_TIME();\n\tconst auto tile = std::make_shared<DynamicTile>(Position(100, 100, 7));\n\tconst auto player = createPlayerAt(tile);\n\tConditionLight condition(CONDITIONID_DEFAULT, CONDITION_LIGHT, 5000, false, 0, 0, 215);\n\n\tASSERT_TRUE(condition.startCondition(player));\n\n\tconst auto state = serializeLightState(condition);\n\tASSERT_TRUE(state.has_value());\n\tEXPECT_EQ(1u, state->level);\n\tEXPECT_EQ(5000u, state->interval);\n\tEXPECT_EQ(1u, player->getCreatureLight().level);\n}\n\nTEST(ConditionLightTest, PreservesValidLevelAndInterval) {\n\tUPDATE_OTSYS_TIME();\n\tconst auto tile = std::make_shared<DynamicTile>(Position(100, 100, 7));\n\tconst auto player = createPlayerAt(tile);\n\tConditionLight condition(CONDITIONID_DEFAULT, CONDITION_LIGHT, 5000, false, 0, 5, 215);\n\n\tASSERT_TRUE(condition.startCondition(player));\n\n\tconst auto state = serializeLightState(condition);\n\tASSERT_TRUE(state.has_value());\n\tEXPECT_EQ(5u, state->level);\n\tEXPECT_EQ(1000u, state->interval);\n\tEXPECT_EQ(5u, player->getCreatureLight().level);\n}\n')
    (ROOT / '.github/workflows/apply-conditionlight-zero-level.yml').unlink()
    Path(__file__).unlink()

if __name__ == '__main__':
    main()
